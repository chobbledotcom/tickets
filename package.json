{
  "name": "tickets",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun@1.3.6",
  "imports": {
    "#fp": "./src/fp/index.ts",
    "#fp/*": "./src/fp/*",
    "#test-utils": "./src/test-utils/index.ts",
    "#test-utils/*": "./src/test-utils/*",
    "#lib/*": "./src/lib/*",
    "#src/*": "./src/*",
    "#test/*": "./test/*"
  },
  "scripts": {
    "start": "bun src/index.ts",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "lint": "bunx @biomejs/biome check --error-on-warnings src test",
    "lint:fix": "bunx @biomejs/biome check --write src test",
    "cpd": "bunx jscpd src --config .jscpd.json",
    "typecheck": "bunx tsc --noEmit",
    "precommit": "bun run typecheck && bun run lint && bun run cpd && bun run test:coverage"
  },
  "devDependencies": {
    "@libsql/client": "^0.17.0",
    "@types/bun": "^1.1.0",
    "jscpd": "^4.0.5",
    "typescript": "^5.8.0"
  }
}
