{
  "AGENTS.md": {
    "semicolon \";\" in Assume the workspace is probably running on NixOS. The developer environment is devenv (%); it provides the pinned Deno and the other tools. Install devenv once (%, or see %), then enter the shell:": 1,
    "semicolon \";\" in Every development machine runs NixOS. Do not use a host-installed % directly. All % commands in this file assume you are already inside %; non-interactive agents must prefix them with %.": 1,
    "semicolon \";\" in Plain language for functional code: Keep the functional style, but name helpers and write comments in simple domain words. Avoid CS jargon in code (%, %, %, %, %, etc.) when a plain phrase works. A helper must explain itself like % Write for someone without a CS degree; a ten-year-old must understand the comment and the method name, even if the implementation uses %, %, or %.": 1,
    "semicolon \";\" in Comments are short, because the code says the rest: Well-named methods and values already say what the code does, so a comment only needs to add what the reader cannot see — a why, a constraint, a surprise. One or two lines is the norm; a paragraph above a few lines of code is a smell, and usually a sign that the code needs to be clearer instead. Never re-narrate the lines below in prose, never restate a name (% above %), and never explain a language feature. If a comment grows to explain a tangle, fix the tangle: rename the thing, or pull the confusing part into a named helper whose name carries the explanation. The bar to clear is this question: is a competent reader surprised or misled without the comment? If the answer is no, delete the comment. This applies to prose in commit messages and PR descriptions too: say what changed and why, then stop.": 1,
    "semicolon \";\" in Zero code duplication: jscpd runs at a non-negotiable 0\\% threshold. Fix duplication with a helper or currying — see Code Duplication. The warning is a positive signal pointing at a real merge to make; never restructure code so the matcher stops matching while two parallel implementations stay standing. Every merge is warranted; the merges are the whole goal.": 2,
    "semicolon \";\" in Always the complete version: When choosing between a result that is less accurate/complete and the full, correct version, always do the complete version — even if it means changing more files than originally estimated. Our aim is always to create the most perfect software; do not ask permission to do it properly.": 1,
    "banned-modal \"would\" in Feature-complete by default: When the open question is whether to take this feature all the way to complete, the answer is yes — build the whole thing, not a partial slice, and do not ask permission to finish it. The only reason to stop short is when finishing would genuinely complicate the codebase: more branching, more special cases, a worse overall shape. That trade-off — completeness against simplicity — is the sole deciding factor; never the effort or the number of files touched. If the two genuinely pull apart, say so explicitly and let the codebase's health break the tie.": 1,
    "semicolon \";\" in Feature-complete by default: When the open question is whether to take this feature all the way to complete, the answer is yes — build the whole thing, not a partial slice, and do not ask permission to finish it. The only reason to stop short is when finishing would genuinely complicate the codebase: more branching, more special cases, a worse overall shape. That trade-off — completeness against simplicity — is the sole deciding factor; never the effort or the number of files touched. If the two genuinely pull apart, say so explicitly and let the codebase's health break the tie.": 1,
    "participle \", leaving\" in Unify systems — the answer is yes: When the question is whether to unify two systems, or collapse two paths into one, the answer is yes. A core aim is to reduce the lines of code needed to accomplish the same thing: the codebase has a finite size it must stay within, so it is critical we reuse and refactor toward one shared mechanism rather than maintain two parallel ones. Two things doing almost the same job are a standing invitation to find the single abstraction that does both — take it. The recent % extraction is the reference: it collapsed the Bunny edge and Deno Deploy entry points onto one shared production request handler, leaving %/% as thin platform wrappers.": 1,
    "semicolon \";\" in Remove dead code — always the answer: When code has no production caller — an unused export, an unreferenced helper, a guard/page whose only consumer is itself unused, an unreachable branch — delete it. Removal is always the right call; never keep it % or % (add it back when the future arrives, from git history), and never paper over it with a test-only import or a lint/usage-check exemption. If a check surfaces an export that is used only by tests, that is a signal the export is dead, not a reason to allow-list it: remove the export (and its now-pointless test). A symmetric-but-unused API is still dead code. The reference: %/% were an agent-only page+guard pair with no route wiring (agents are gated via %/%), so both were deleted rather than exempted.": 1,
    "semicolon \";\" in Keep code and test files under ~400 lines: When refactoring a code or test file, aim to keep it under 400 lines — and if hitting that target means splitting one file into several, so be it: a new file is cheaper than an overloaded one. When you end up with a handful of files all about the same thing, group them in a folder and give them shorter names that do not repeat the folder's name (%, not % — see the % and % examples in Designing new systems). While you are at it, use the split as a chance to separate pure from non-pure code — push the data-in/data-out logic into its own file and keep the IO in a thin shell (see Pure, functional). The same 400-line limit applies to test files, and matters just as much: smaller, more specific test files let us run mutation tests far faster, because a source file's mutants only need to run against the narrow test file that covers it, not one giant suite. Biome enforces a hard 1,000-line ceiling for every code and test file; never add an override to let one past it. % holds the 400-line aim over every source tree, against the accepted list at % that records where each over-limit file stands. The list only shrinks. Root instruction files such as % are exempt because their policy must be available as one automatically loaded document, but their sections must still stay concise. (Expect a known side effect when splitting: jscpd cannot fully scan very large files, so a split routinely surfaces duplication that was silently passing inside the monolith — budget for extracting helpers, not just moving tests.)": 1,
    "banned-modal \"would\" in A written-down diagnosis is a hypothesis, not a finding: A GitHub issue, a review comment, a commit message, or a code comment explaining why something breaks was written by someone reasoning about the code at a moment that has passed. Re-derive it from the current source before you fix anything, however confident and detailed it reads — a wrong diagnosis is more expensive than none, because it aims your fix at the wrong place and takes the regression test with it. The stripe-mock port-steal note (%) is the worked example: it was a careful, plausible, thoroughly argued account of a race in the wrong function, and following it would have % code that was already correct while leaving the real hazard in place. When you find one wrong, correct the note in the same change — leaving it sends the next person down the same path.": 1,
    "semicolon \";\" in A finished job closes its issue: GitHub issues hold work that is still open. When you complete one, close it in the same change. The commit message and the pull request are the record of what you did. Never leave an issue open and marked %, %, or %. A reader must be able to trust that every open issue is work somebody can still pick up. The same applies to an issue you did not write. When you find one the code already answers, check it against the current source. Then close it with a comment that names the answer. An issue stays open only when part of its work is still open. % was the predecessor of the issue tracker. Its entries that named work moved to GitHub issues; its entries that recorded a decision or a contract moved to %; the file is now deleted. Its old rule still holds for issues: close an issue when you finish it, and never leave one marked %. The entry this file cites as a worked example, the stripe-mock port-steal note, is documentation, not a job, and lives at %.": 2,
    "banned-modal \"would\" in Every bug fix ships with a regression test: Never fix a bug without also adding a test that fails before the fix and passes after it. The test must exercise the real bug — reproduce the exact condition that was broken so it would have caught the original defect — not merely touch the changed lines for coverage. Write the failing test first, confirm it fails for the right reason, then apply the fix and watch it go green. This locks the bug out for good and proves the fix actually addresses it.": 1,
    "banned-modal \"would\" in A branch fixes the defects it introduces: A review finding about code the branch adds is that branch's work. Fix it in the branch, pin it with a regression test, and merge the branch clean. Never record it as a follow-up issue and merge anyway: the defect exists nowhere but the open branch, and the issue hands work to a future person who must rebuild the context in front of you now. An issue is for a defect that is already on main, or for one that is genuinely outside the branch's scope. If the fix would complicate the branch, split the branch instead of merging a known defect. In a stack, the fix belongs to the layer that introduces it.": 1,
    "semicolon \";\" in One path for one-or-many — a single item is an array of one: Do not write a separate % code path beside a % one (no % next to %, no % branch that renders/loads/books differently from the N-item case). Model the operation over a collection once and call it with an array of one when there is a single item; derive the singular answer from the array result (%). A thin singular wrapper that delegates to the array implementation is fine (it is still one path); two parallel implementations that can drift are not. The multi-group membership refactor is the reference: a listing's groups are always an array, never a special-cased single %. This keeps behaviour identical for 1 and N, and kills the class of bug where the single case is fixed but the batch case is not (or vice-versa).": 2,
    "semicolon \";\" in Never render a dead or forbidden link: Do not emit a link the viewer cannot follow — one whose target returns 404, or whose page the current user's admin level cannot open. A rendered link is a promise that it works, so gate it on the same condition the target enforces; when that condition fails, show plain text or an indicator in its place rather than a link that breaks on click. The no-quantity attendee's ticket cell is the reference: a quantity-0-only attendee has no live % page (it 404s), so admin views render a % indicator instead of the % link. This holds for permission-gated links too: an action a role cannot reach must not be linked for that role. Mind the blind spot — a link to a restricted page still works when the page is viewed (or tested) as a high-privilege user, so the dead link the lower-privilege roles see goes unnoticed. Gate the link on the same permission the target enforces, and when testing visibility, render the page as each role rather than only the most-privileged one.": 1,
    "semicolon \";\" in Operator decides genuine conflicts — a required choice, never a silent default: When an action hits a conflict the system cannot unambiguously resolve (for example an attendee merge where both records booked the same listing, or where each side carries a real payment), do NOT auto-pick a resolution and quietly proceed. Surface the conflict and make the operator choose explicitly via a required field — the request fails closed until they decide. Silently moving money, voiding a leg, or keeping one side by default hides a real decision behind a guess; an explicit operator choice keeps the irreversible call — especially anything that touches the money ledger — with the human who can see the context.": 1,
    "semicolon \";\" in Name positional results at the boundary: When a library returns an ordered array of different results, destructure it into domain names as soon as it enters our code. Keep the unavoidable ordering beside the call that creates it; do not make readers trace % or % through later mapping code. If the number of results is not guaranteed, validate it at that boundary before naming the values.": 1,
    "semicolon \";\" in Never lose work — commit WIP even if broken: Uncommitted changes are lost if the working environment is reclaimed (it has happened). If you have non-trivial work in progress and are about to pause, hand off, delegate to a background agent, or end a turn with a dirty tree, commit and push it rather than leaving it uncommitted. A known-broken checkpoint is fine and expected — mark it unmistakably in the commit message (for example %) so it is never mistaken for finished work, and follow up with a green commit. Do not hold a commit back purely because the tree does not yet build or pass; losing the work is worse.": 1,
    "semicolon \";\" in % outranks %: A finding, review comment, or idea earns a fix when it names a concrete failure — real inputs and state under which behaviour is wrong: money lost or double-moved, data corrupted, a crash, a dead or forbidden link, a permission hole. Verify the scenario against the real code first; verified breakage gets the complete fix and its regression test. Everything else — symmetry, hypothetical drift with no concrete trigger, restating what a pinned test already enforces, polish whose only effect is making the document or code % — is perfect-world work: declining it with a short reason on the thread is a first-class outcome, and a genuinely good idea becomes a GitHub issue rather than queue work. This is triage for what enters the queue, never licence to do entered work partially — % still governs everything we decide to do. Assertive automated reviewers can generate perfect-world findings indefinitely, so bot silence is not a finish line: when consecutive rounds stop naming new concrete failures, declare the review converged and hand the decision to a human.": 1,
    "present-perfect \"has been\" in While a layer is above another open layer, run targeted mutation tests for the source changed by that layer. % compares the whole branch with %, so run that branch-level gate after the lower layers merge and the layer has been rebased into the bottom position.": 1,
    "semicolon \";\" in This codebase practices offensive programming, not defensive programming. Defensive code tolerates bad states to keep running; offensive code makes bad states impossible to miss. A loud failure is almost always better than a silent wrong answer — the crash points at the bug, while a swallowed error corrupts data far from the cause. % and % in Preferences are this same philosophy; the rules below are how it applies to everyday error handling.": 2,
    "semicolon \";\" in A missing expected field from structured external data is a HARD no to default away. JSON API response, database row, config, env var, webhook payload, fetch result, file/CLI output — if the field is documented/expected, missing means something is wrong upstream, and the program must fail there, not invent a value. Validate at the boundary with a valibot schema and pass typed values inward (%); where a schema is overkill, check and throw the way % does (% — a gateway response without a message id throws, it does not return %) and % does for a missing % (%).": 1,
    "semicolon \";\" in Sentence case, not Title Case. Capitalise the first word and proper nouns only — %, %, % — for buttons, labels, headers, and messages alike. Some older keys are still Title Case; align them to sentence case when you next touch that surface.": 1,
    "semicolon \";\" in End full sentences with a full stop; never a label, button, or column header. A message that is a sentence gets its full stop; a fragment used as a control does not.": 2,
    "semicolon \";\" in % (run inside %) scans the catalog and fails on the mechanical rules a machine can judge: descriptive links (never % / %; the link text names where it goes) and even spacing (no double spaces; literal %/% examples are exempt). Each finding names its fix. The checker is a floor, not the whole rule. It cannot tell whether a sentence runs too long or a word is too fancy — that judgement is yours on every copy change, which is what the rest of this section is for.": 2,
    "semicolon \";\" in The curried helpers actually exported from % are documented on each definition in %; read those before you hand-roll a collection step. Several are thin adapters over %. For a collection operation % does not cover, use % directly, and wrap it in a curried % adapter once more than one caller needs it. % has no % export (it was removed in favour of the runtime built-ins) — use native % / %, or %'s %.": 1,
    "semicolon \";\" in Every merge is warranted — the merges are the goal. When jscpd flags a new helper against an existing one (as it will the moment you extract something), the two are the same operation and must be unified into a single mechanism. Reducing the codebase to one shared way of doing each thing is the aim; the warning is just the to-do list.": 1,
    "banned-modal \"would\" in A curry almost always exists — % is nearly always wrong. Two functions that differ only in a value, a path, a field name, a message, or a callback are one function that has not been given its parameter yet. This holds even when the two bodies look nothing alike at a glance, because the shared part is often a tail (%) or an opening (%). Treat every flagged pair as mergeable until you have actually written the curry and found what the parameter would have to be. Before you write one, look for the factory that already exists: an under-adopted curry reads exactly like unavoidable duplication, and the Cucumber page openers (% in %) are the reference.": 1,
    "banned-modal \"would\" in An entity cache that also backs single-record reads. When one request-scoped cache serves both the collection view and the %/% detail/auth reads (listings, users, groups, holidays, built-sites, attendee-statuses), it loads the full entity once so the detail, edit, and login paths it feeds have every column. Narrowing the cache load would break those reads. (%' % is deliberately wide — it also carries the trigger-maintained %/%/% aggregate columns.)": 1,
    "semicolon \";\" in A table's whole-row read (%/% with no columns named, in %) — it selects every stored column by design and feeds edit pages that need the whole row; a read that wants less names its columns with %, and specific tables narrow at the cache % layer instead.": 1,
    "semicolon \";\" in % - Run the server with %, restarting it whenever a source file changes. % runs once at the start, so an edit to a static asset still needs the task restarted. The dev database is the gitignored % at the repo root (its key lives beside it in %), so data survives restarts; delete both files to start fresh. % is not a valid dev URL — interactive transactions open a second connection, and each in-memory connection is its own empty database": 1,
    "semicolon \";\" in % - Run only cases with declared screenshot captures, one at a time, and write the versioned manifest plus PNG assets under %; the task requires a clean Git worktree so the manifest commit matches the captured code": 1,
    "semicolon \";\" in % - Format Markdown with Deno, then format and lint code with Biome (%; auto-fixes in place). Format through this task rather than reaching for a formatter yourself. The two own different file types and neither refuses work outside its own, so % pointed at a directory of TypeScript quietly reformats every file it finds to a style Biome does not use — an enormous unrelated diff that buries the change you meant to make. Naming one file directly is fine when you know whose it is: % for Markdown, Biome for code. When in doubt, run the task.": 1,
    "banned-modal \"would\" in % - Strict, read-only formatting and lint. Runs % for Markdown and Biome % for code. Fails on lint warnings (for example cognitive complexity) and on any file that would be reformatted, without touching the checkout. This is the lint % runs in every environment, so a clean % locally means the lint step will pass in CI too. Run % to auto-fix before re-running.": 1,
    "semicolon \";\" in % - Dump the database out-of-band to a % (uploads to the configured storage zone by default; pass % for a local file). Runs in a full Deno process with no per-request subrequest budget, so it can dump arbitrarily large databases.": 1,
    "semicolon \";\" in Cucumber owns user journeys and observable business rules; direct Deno tests own pure logic, technical contracts, and everything a story cannot prove. A Cucumber journey never supplies the only coverage of a production line or branch.": 1,
    "banned-modal \"would\" in Treat 100\\% coverage as a hygiene floor, not proof that tests would catch meaningful regressions.": 1,
    "semicolon \";\" in Avoid compound boolean assertions such as %; assert the observable contract directly with exact values, object shape, persisted rows, HTTP status/body, or rendered content.": 1,
    "banned-modal \"would\" in % only guesses which assertions look weak. % proves it: it mutates operators in your source and checks whether your tests fail. A mutant your tests still pass on (%) is a real gap — a code change nothing would have caught.": 1,
    "banned-modal \"would\" in As a manual tool it is targeted (run % on the module you are hardening) — running it across the whole tree would be far too slow. % runs it automatically, but only over the files this branch changed (its committed diff against %/%), and demands a 100\\% kill rate. Run it before merging a branch that changes % files; the standard % no longer runs it (it was too slow for every commit).": 1,
    "semicolon \";\" in As a manual tool it is targeted (run % on the module you are hardening) — running it across the whole tree would be far too slow. % runs it automatically, but only over the files this branch changed (its committed diff against %/%), and demands a 100\\% kill rate. Run it before merging a branch that changes % files; the standard % no longer runs it (it was too slow for every commit).": 1,
    "semicolon \";\" in When a manual mutation run (or the precommit gate) surfaces survivors on a file you are touching — even on lines you did not change in this PR — they are yours to fix. Never determine whether a survivor % (no %, no diffing against the base to excuse it): the bar is 100\\%, and a survivor on a line in your changed file is a real gap in that file's tests that you are now the person best placed to close. Either write the assertion that kills it, or record the mutant in % with a proof that no input can distinguish it. % is not a resolution; leaving it just guarantees the next person trips over the same survivor. This is the Good citizen rule applied to mutation testing. The gate is a best-effort check with two documented blind spots (see the header of %): it scopes to the committed diff, so uncommitted work is not checked until committed. It also diffs against your local % and never re-fetches, so a stale local ref under a branch built on newer main commits can leak upstream src into the set (run % first — a branch's own author is unaffected). In each case, reach for % on the specific module.": 1,
    "semicolon \";\" in Never run repo tooling as a subprocess inside a test. jscpd, Biome, and typechecking are dedicated precommit/CI steps; a test that shells out to % re-runs a minute of CPU inside every suite run to enforce a gate that already exists elsewhere.": 1,
    "semicolon \";\" in Keep heavy SDKs out of module load. Every test isolate — a group of files under the full runner, each named file under % — evaluates the whole app module graph, so an import-time SDK evaluation is paid once per isolate, dozens of times per run. Dynamically import heavy dependencies on first use; % and % are the references (this is the cold-start rule from Designing new systems applied to tests).": 1
  },
  "CLAUDE.md": {},
  "E2E_TESTS.md": {
    "semicolon \";\" in How this repository writes Cucumber acceptance specifications and the direct tests that live alongside them. AGENTS.md covers everything else; this file is the detail for anyone writing, migrating, or reviewing a story.": 1,
    "present-perfect \"have been\" in Read Migrating a test to a story before turning an existing test into a Feature — the checklist there exists because the same mistakes have been made repeatedly.": 1,
    "banned-modal \"Would\" in Migrate e2e tests toward Cucumber or direct contracts. When touching an e2e test, ask: can the claim be stated as an actor-facing rule without technical nouns? If yes, move the narrative to Cucumber and delete the old test. Is the production behavior fully determined by explicit values? If yes, keep or extract a pure function and test it directly. Would replacing the real boundary make the assertion stop proving its subject? If yes, keep a direct technical contract test. Split tests that mix all three concerns by claim rather than duplicating.": 1,
    "semicolon \";\" in Every Feature has one known %, %, at least one %, and at least one % tag. Other tag kinds come from the checked registry; ad-hoc metadata tags are forbidden.": 1,
    "semicolon \";\" in Validate DataTable rows, DocStrings, custom parameters, and Examples cells at the boundary with a typed schema. All table cells begin as strings; never cast and hope.": 1,
    "semicolon \";\" in Every step must match exactly one definition. Undefined, ambiguous, pending, skipped, and retried steps fail. The full suite also fails on unused step definitions; focused runs can leave unrelated shared definitions unused.": 1,
    "semicolon \";\" in Run Scenarios through the bounded Cucumber worker pool. Each worker has its own isolate, and every Scenario gets a fresh database and World. Test environment changes must use % so % and the worker's % stay aligned. Do not add retries; a pass after retry is still a flaky failure.": 1,
    "semicolon \";\" in Reuse the existing golden database, stripe-mock, static assets, encryption, browser, cache reset, and cleanup mechanisms. Extract one hook-free fixture when Cucumber and Deno hooks need the same lifecycle; never maintain two.": 1,
    "semicolon \";\" in Screenshot evidence uses those authored story, rule, and case IDs plus a separate stable capture ID. Its public contract is % with files under %; raw Cucumber Messages stay generated and private.": 1,
    "semicolon \";\" in Deliberately dropped, because the claim was about the old test's own scaffolding rather than the product. Say so out loud; do not let it vanish silently.": 1,
    "wordy \"simply\" in that a race was settled by capacity rather than by one request simply failing,": 1,
    "semicolon \";\" in every field you are about to send is rendered on the page;": 1,
    "semicolon \";\" in any value chosen from a dropdown is one of the options offered;": 1,
    "semicolon \";\" in % does all of this; use it rather than writing a new check. Two traps it exists because of: matching % misses markup that writes % first, and looking for % anywhere inside a % wrongly rejects a perfectly normal disabled placeholder option.": 1,
    "banned-modal \"would\" in A refusal must leave nothing behind. Prove the row is absent, not just that a later booking still fits. A scenario with a limit of 3 refused a 2-place booking and then probed with 1 place: had the refused booking leaked a row, the probe would still have passed on the one place left over. Assert the refused thing does not exist.": 1,
    "banned-modal \"would\" in A % that calls the same production helper that produced the value proves nothing: change the helper and both sides move together. A download's date column was checked with %, the very function that writes it, so changing its exclusive-end arithmetic would have kept the story green. Write the expected value out in the step from the story's own numbers.": 1,
    "banned-modal \"would\" in Two definitions matching one step text is an Ambiguous error, and a generic % step will collide with the literal ones already registered. Before adding a generic step, grep for the literals it would swallow. Merge them only when their assertions are genuinely the same; if a literal asserts more (a conservation check, an extra surface), keep it and word the new step differently.": 1,
    "semicolon \";\" in Two definitions matching one step text is an Ambiguous error, and a generic % step will collide with the literal ones already registered. Before adding a generic step, grep for the literals it would swallow. Merge them only when their assertions are genuinely the same; if a literal asserts more (a conservation check, an extra surface), keep it and word the new step differently.": 1,
    "banned-modal \"would\" in Plain language must still be true. % described a choice that returns nothing to the customer's card — it records credit owed to them. A reader would have learned the opposite of the truth. When naming a money decision, a status, or an outcome, check what the code does before you name it.": 1,
    "banned-modal \"would\" in The Feature's own prose is a claim. The narrative under % and the paragraph under each % are published in the story catalog as statements about the product, and nothing executes them. A Feature description promising that % survived after the scenario that would have tested it was removed — so the catalog would have published a guarantee the product does not give, with every scenario passing. Describe only what the Rules below actually exercise. When you remove a scenario, re-read the prose above it.": 2,
    "banned-modal \"could\" in When the story fails because the product does not do that, do not soften the story. This is the most valuable thing a migration finds, and the temptation is to reword the % until it goes green. Instead: delete the scenario, leave the original test where it is, and write the question up in a GitHub issue with what you expected, what the product does, and the ways it could be settled. Say it in the pull request too. A scenario weakened to match behaviour nobody chose is worse than no scenario — it makes the accident look intended.": 1
  },
  "PR_WORKFLOW.md": {
    "semicolon \";\" in Add the following sections to the feature plan. Use tables where there is more than one case. Write % when a section truly does not apply; do not omit it.": 1,
    "semicolon \";\" in who can perform each action;": 1,
    "semicolon \";\" in which links each role can see;": 1,
    "semicolon \";\" in which secrets or personal fields cross each boundary;": 1,
    "semicolon \";\" in when sensitive facts can be redacted;": 1,
    "semicolon \";\" in the current-system value and production caller;": 1,
    "semicolon \";\" in the old path deleted or replaced;": 1,
    "semicolon \";\" in expected source files and source-line budget;": 1,
    "semicolon \";\" in database and provider call budget;": 1,
    "semicolon \";\" in its place in a stack;": 1,
    "semicolon \";\" in the current-system value;": 1,
    "semicolon \";\" in trusted facts and observed facts;": 1,
    "semicolon \";\" in valid states and state transitions;": 1,
    "semicolon \";\" in failures, retries, replays, and races;": 1,
    "semicolon \";\" in genuine owner choices;": 1,
    "semicolon \";\" in security and privacy boundaries;": 1,
    "semicolon \";\" in proposed vertical PRs and source budgets;": 1,
    "semicolon \";\" in the tests that will prove each contract row;": 1,
    "semicolon \";\" in keep one source of truth;": 1,
    "semicolon \";\" in let invalid expected data throw at its boundary;": 1,
    "semicolon \";\" in preserve genuinely unavailable or pending states;": 1,
    "semicolon \";\" in do expensive or external work outside database transactions;": 1,
    "semicolon \";\" in make the final state change atomic;": 1,
    "semicolon \";\" in do not add compatibility wrappers for internal callers;": 1,
    "semicolon \";\" in the direct test file for the changed rule;": 1,
    "semicolon \";\" in the focused route or integration test;": 1,
    "semicolon \";\" in the affected Cucumber feature;": 1,
    "semicolon \";\" in Do not repeatedly run the full suite or mutation gate while the behavior is still changing. Those checks prove a stable candidate; they are expensive ways to discover that the design is unfinished.": 1,
    "semicolon \";\" in review finds two cross-cutting state or concurrency bugs;": 1,
    "semicolon \";\" in two routes need slightly different versions of the same rule;": 1,
    "semicolon \";\" in a new optional field changes meaning by state;": 1,
    "semicolon \";\" in the same failure is handled in more than one layer;": 1,
    "semicolon \";\" in tests require many special cases to set up one state;": 1,
    "semicolon \";\" in every contract row has production code and a test;": 1,
    "semicolon \";\" in every new export has a production caller;": 1,
    "semicolon \";\" in no old parallel path remains;": 1,
    "semicolon \";\" in no failure becomes %, empty, unpaid, or successful by default;": 1,
    "semicolon \";\" in SQL selects only needed columns and writes complete state atomically;": 1,
    "semicolon \";\" in links have the same permission and existence rules as their targets;": 1,
    "semicolon \";\" in user copy is plain, translated, and consistent;": 1,
    "semicolon \";\" in comments describe current constraints rather than development history;": 1,
    "semicolon \";\" in the source-line count remains below the limit;": 1,
    "banned-modal \"would\" in Commit any final source changes before running %. The mutation gate scopes to the branch's committed diff (%) and deliberately ignores the worktree and index, so uncommitted edits are not mutated and would only be covered by a later commit's ordinary pre-commit gate, which does not re-run mutation testing. After any later commit, rerun % to restore that coverage.": 1,
    "semicolon \";\" in Do not use mutation testing to invent the behavior contract. It can show that an existing assertion is weak; it cannot show that a missing state, race, or external failure was never designed.": 1,
    "semicolon \";\" in Rebase or synchronize the branch with its current parent before running the final gates in step 12. If a lower stacked layer changed or merged since step 12 ran, the rebase changes the final diff, so rerun both % and % afterward; CI does not run mutation testing, so watching it does not replace the post-rebase gate.": 1,
    "semicolon \";\" in behavior added or replaced;": 1,
    "semicolon \";\" in current-system value and exact production caller;": 1,
    "semicolon \";\" in trusted and observed facts affected;": 1,
    "semicolon \";\" in old path deleted;": 1,
    "semicolon \";\" in source and total changed-line counts;": 1,
    "semicolon \";\" in database and provider call counts where relevant;": 1,
    "semicolon \";\" in tests and mutation commands run;": 1,
    "semicolon \";\" in every planned contract row is implemented and tested;": 1,
    "semicolon \";\" in the live system has one path for the changed behavior;": 1,
    "semicolon \";\" in invalid states are rejected at their boundary;": 1,
    "semicolon \";\" in stale and concurrent work cannot overwrite newer state;": 1,
    "semicolon \";\" in interrupted external work has a durable recovery path where required;": 1,
    "present-perfect \"has been\" in every review thread has been read, answered, and resolved;": 1,
    "semicolon \";\" in every review thread has been read, answered, and resolved;": 1,
    "semicolon \";\" in the branch is current with its parent;": 1,
    "semicolon \";\" in source churn is below the limit;": 1,
    "semicolon \";\" in final local gates and CI pass;": 1
  },
  "README.md": {
    "semicolon \";\" in Bulk email: compose/preview/send to filtered attendee groups with reusable subject/body templates (encrypted under the owner key); recipients get one-click unsubscribe / resubscribe / erase links": 1,
    "semicolon \";\" in Ntfy error notifications for production monitoring (optional; sends domain + error code only, never PII)": 1,
    "semicolon \";\" in Optional postcode/address lookup on booking and admin forms, via a same-origin proxy that keeps the provider key server-side, caches results encrypted, and rate-limits anonymous use; provider is pluggable": 1,
    "semicolon \";\" in Self-updating fleet: each site records its build commit; the host sees which sites are behind and redeploys them from the latest GitHub release": 1,
    "banned-modal \"would\" in A database dump alone is not sufficient to recover PII - an attacker would also need the encryption key from the environment": 1,
    "semicolon \";\" in Themes are applied through the site's custom CSS form. The choices are %, %, %, and %; use % to capture every selected page in every theme. Set % when Chromium is not in Playwright's normal browser cache. On NixOS, % provides Chromium and sets this variable for you.": 1,
    "semicolon \";\" in External scenario modules own one specific screenshot's data, custom CSS, and browser steps while reusing this runner's temporary site and high-resolution capture. Pass their path with %; the scenario's % sets the PNG filename directly inside %. A scenario can set its own viewport when a wider layout is part of the screenshot. Set % when the scenario needs to capture the site before setup. The runner skips admin setup, Stripe, and the saved theme, then applies the scenario's CSS after its browser steps.": 1,
    "semicolon \";\" in The task reads % and % from %. Values in that file take priority over existing shell variables. It then uses libSQL page-level sync, moves all WAL data into the main file, and checks the finished database before publishing it. It refuses to replace an existing file. The temporary replica is created beside the output and removed on success or failure. Local % files and % are ignored by Git. Run this only on a development machine; it needs a local filesystem and the native libSQL client. The file contains the database exactly as stored, including encrypted personal data, but it does not include environment secrets or files held in Bunny Storage.": 1,
    "semicolon \";\" in The task checks that the destination name is free before downloading the source. It creates a verified standalone SQLite snapshot, then streams that file through Turso's native database upload API. It does not rebuild the database one SQL statement at a time. If token creation or upload fails, it deletes the incomplete Turso database. On success it prints the new % and %. Keep using the source %; that key is not stored in the database file.": 1,
    "banned-modal \"would\" in % (manual) — upgrades the built client sites that accept the published tier. It asks the main instance for the DB credentials of the sites on that tier (each site's own full-access token — the workflow only reads, to take backups), backs each up to the builder's storage, then deploys. The % run input (% default, then % or %) is the release channel being published: each site carries its own % channel and only the sites at the deploy's tier or more eager are returned — a % deploy reaches every site, % reaches beta + alpha sites, % only alpha sites. The default is % (the smallest blast radius), so you ship a risky build to alpha sites first and reaching the whole fleet is the deliberate % choice. For a canary (%/%) the workflow fails closed unless the main instance echoes the tier back in its response, so running it against a master that predates tier filtering (which would ignore the filter and return the whole fleet) cannot silently deploy to everyone — upgrade the master first. It needs the % URL secret plus % and %; CDN-enabled builds additionally need the five optional CDN secrets below. The % that authorizes the credentials endpoint is pasted in as a run input each time, never stored (set the same value as the main instance's % env). % is no longer needed — the script ids come from the endpoint.": 1,
    "semicolon \";\" in % (manual) — upgrades the built client sites that accept the published tier. It asks the main instance for the DB credentials of the sites on that tier (each site's own full-access token — the workflow only reads, to take backups), backs each up to the builder's storage, then deploys. The % run input (% default, then % or %) is the release channel being published: each site carries its own % channel and only the sites at the deploy's tier or more eager are returned — a % deploy reaches every site, % reaches beta + alpha sites, % only alpha sites. The default is % (the smallest blast radius), so you ship a risky build to alpha sites first and reaching the whole fleet is the deliberate % choice. For a canary (%/%) the workflow fails closed unless the main instance echoes the tier back in its response, so running it against a master that predates tier filtering (which would ignore the filter and return the whole fleet) cannot silently deploy to everyone — upgrade the master first. It needs the % URL secret plus % and %; CDN-enabled builds additionally need the five optional CDN secrets below. The % that authorizes the credentials endpoint is pasted in as a run input each time, never stored (set the same value as the main instance's % env). % is no longer needed — the script ids come from the endpoint.": 1,
    "semicolon \";\" in Optional static CDN builds: set all five repository secrets %, %, %, %, and % to publish site-independent browser bundles and image-codec WASM to a dedicated Bunny Storage/Pull Zone during the build. % is the Storage API hostname shown on the zone's Bunny Storage Access page, such as % or %; it has no default because it must match the zone's primary region. The existing % secret purges that pull zone after every successful upload. Each upload carries its SHA-256 checksum, and the build then fetches every public URL and verifies its bytes and media type before emitting a script that depends on it. The generated script contains immutable, content-addressed CDN URLs and the matching CSP origin; these values are not runtime Bunny script secrets. Configure the pull zone's browser cache override for a long lifetime because these paths change whenever their content or media type changes. Site-bound assets such as the embed bootstrap and dynamic order widget remain in each script. If all five CDN secrets are absent, the build remains fully self-contained. A partial configuration fails the build. Historical restore builds intentionally receive none of these secrets and therefore retain their embedded assets.": 2,
    "semicolon \";\" in Restoring to a point in time: the running build records the git commit it was built from into its own % table (%, written on boot; cleared if a build ships without one, so it never goes stale), so every dump carries the commit the site was running when the backup was taken. Restoring a backup rolls back the data and surfaces that commit (the full SHA); to roll the code back to match, run the % workflow with that commit and the target script id. It builds the edge bundle fresh from our repository at that commit (git is the per-commit source of truth) and deploys it — so a restore only ever redeploys a commit from our own history, never code carried inside an (untrusted) uploaded backup file. For safety the workflow validates the commit is a full SHA and deploys via the external Bunny action pinned to an immutable SHA, so the production API key is never handed to a deploy wrapper loaded from the checked-out (possibly old) commit. (The rebuilt old code reports the rebuild time as its build version, so the in-app updater may not surface the latest release afterwards — redeploy latest via the normal deploy workflow to return to it.)": 2,
    "banned-modal \"may\" in Restoring to a point in time: the running build records the git commit it was built from into its own % table (%, written on boot; cleared if a build ships without one, so it never goes stale), so every dump carries the commit the site was running when the backup was taken. Restoring a backup rolls back the data and surfaces that commit (the full SHA); to roll the code back to match, run the % workflow with that commit and the target script id. It builds the edge bundle fresh from our repository at that commit (git is the per-commit source of truth) and deploys it — so a restore only ever redeploys a commit from our own history, never code carried inside an (untrusted) uploaded backup file. For safety the workflow validates the commit is a full SHA and deploys via the external Bunny action pinned to an immutable SHA, so the production API key is never handed to a deploy wrapper loaded from the checked-out (possibly old) commit. (The rebuilt old code reports the rebuild time as its build version, so the in-app updater may not surface the latest release afterwards — redeploy latest via the normal deploy workflow to return to it.)": 1
  },
  "REPO_STRUCTURE.md": {},
  "cli/README.md": {},
  "docs/cold-start.md": {
    "semicolon \";\" in import the shared session-key error without evaluating the authentication feature;": 1,
    "semicolon \";\" in load admin API authentication only after its path matches;": 1,
    "semicolon \";\" in load migration execution only for database-backed requests;": 1,
    "semicolon \";\" in keep the generic page layout independent of storage; and": 1,
    "semicolon \";\" in Group liveness no longer imports the 900-line payment-flow module just to use two loaders. In the one-file deploy this defers evaluation work; it does not reduce file parsing.": 1
  },
  "docs/comment-policy.md": {
    "banned-modal \"could\" in This measured what the comments in % cost, tested the three claims that prompted the question, priced every cap we could set, and now records the decision taken and what is left to do.": 1,
    "banned-modal \"may\" in Decided and shipped. A comment may run at most 12 lines, and one of its lines at most 100 columns. % enforces both in %; the numbers live in % and are meant to come down. See Where we are for the remaining steps.": 1,
    "semicolon \";\" in Decided and shipped. A comment may run at most 12 lines, and one of its lines at most 100 columns. % enforces both in %; the numbers live in % and are meant to come down. See Where we are for the remaining steps.": 1,
    "semicolon \";\" in 9,744 comment blocks, which is the same corpus with each run of consecutive % lines merged into one. Only the % figure uses this; nothing is priced against it.": 1,
    "semicolon \";\" in 49.4\\% sit in columns 70–80, the band you only land in by deliberately filling to the margin;": 1,
    "semicolon \";\" in 21 stale identifiers — %, %, %, % and friends exist only in the comment that mentions them;": 1,
    "semicolon \";\" in 1. It aims at the wrong half of the corpus. A count cap treats a 30-line docstring and a five-word note as one thing each. A file with a single 44-line header passes a cap of 1; a file with six sharp one-liners fails it. The cap would delete the cheap, accurate comments and leave the long prose that actually drifts.": 1,
    "banned-modal \"would\" in 1. It aims at the wrong half of the corpus. A count cap treats a 30-line docstring and a five-word note as one thing each. A file with a single 44-line header passes a cap of 1; a file with six sharp one-liners fails it. The cap would delete the cheap, accurate comments and leave the long prose that actually drifts.": 1,
    "semicolon \";\" in A cap of 10 is met today by shuffling; a cap of 5 is met by doubling the file count. Neither outcome is the one we want.": 1,
    "banned-modal \"would\" in 3. Directives eat the budget. 309 files (26\\%) carry comments that are not prose at all, and 299 of them carry two or more — a % / % pair around an import block, in 263 files. One file carries seven. Any cap under 3 is unmeetable for a quarter of % unless directives are exempt, which any real rule would do anyway.": 1,
    "semicolon \";\" in Directives exempt throughout. % is how many files fail on day one; % is roughly how much prose has to go. Every figure here is the whole cost of adopting that rule, not a per-step one — the staged table further down splits the same totals into per-PR instalments.": 1,
    "banned-modal \"would\" in What a strict limit would actually cost": 1,
    "banned-modal \"would\" in It would delete comments % holds up as exemplary. The guide cites % under % — and that file's docstring runs 22 lines. Its load-bearing sentence (%) is one line; the other twenty re-narrate the code below. So the tension is real but mostly compressible — which is the argument for a length cap rather than a ban.": 1,
    "semicolon \";\" in It would delete comments % holds up as exemplary. The guide cites % under % — and that file's docstring runs 22 lines. Its load-bearing sentence (%) is one line; the other twenty re-narrate the code below. So the tension is real but mostly compressible — which is the argument for a length cap rather than a ban.": 1,
    "semicolon \";\" in Three existing rules mandate comments. % requires a comment on an empty % whose fallback is the contract (61 % sites in %), a % note on % returns (139 mentions across 12 files), and a reason at any hand-rolled utility. % is read by %. None of these is threatened by a length cap; all of them are threatened by a tight count cap.": 1,
    "banned-modal \"would\" in % output would shrink. % and the eleven barrels in % exist only to generate API documentation from jsdoc — 67\\% of % comments are jsdoc, and they are what a reader sees on hover. Those barrel files are almost entirely % prose and must be exempt from any ratio rule or they fail by construction.": 1,
    "semicolon \";\" in File-size pressure eases. % has 54 files over the 400-line guideline; on code lines alone it would be 29. Comments are what push 25 files past it.": 1,
    "banned-modal \"would\" in File-size pressure eases. % has 54 files over the 400-line guideline; on code lines alone it would be 29. Comments are what push 25 files past it.": 1,
    "semicolon \";\" in hits the half of the prose that actually drifts, and leaves 7,126 accurate one-liners alone;": 1,
    "semicolon \";\" in cannot be gamed by splitting a file;": 1,
    "semicolon \";\" in leaves every mandated comment (%, %, hand-rolled reason, %) legal;": 1,
    "semicolon \";\" in Land it by lowering one number, not by grandfathering files. The repo has no allow-lists and must not gain one. Set the cap in config at a number that passes today and lower it per PR; each step is bounded.": 1,
    "banned-modal \"would\" in (The ≤ 16 step, omitted for brevity, adds 55 comments between ≤ 20 and ≤ 12.) Each step's own cost falls and rises because the comments are not spread evenly across lengths — ≤ 4 catches a large cluster, ≤ 3 a smaller one. The last three steps are still large and would split by directory.": 1,
    "semicolon \";\" in Biome has no rule for any of this, so enforcement is a script. % in % already does the parsing; a checker plus its % step is roughly 80 lines.": 1,
    "present-perfect \"had been\" in The count of over-length comments was slightly understated, because the lexer was mis-parsing regular expressions and lost its place in three files. Fixing it revealed three comments in % and % that had been hidden.": 1
  },
  "docs/designing-systems.md": {
    "semicolon \";\" in Model the thing as data first — a typed schema plus a few functions folded over it — and derive everything else (types, validation, rendering, routes) from that one declaration. The philosophy is in the Preferences of AGENTS.md (%, %); these are the mechanisms to copy:": 1,
    "semicolon \";\" in A valibot schema as the single source of truth for a value type. Declare once; derive the TS type, the runtime guard, and the options list:": 1,
    "semicolon \";\" in Schema-driven forms. % + a % (%): one field list drives the HTML rendering, the parsing/validation, and the % value types; %/% (%) wire that same schema to both the GET (render) and POST (validate) handlers.": 1,
    "semicolon \";\" in A data table plus one fold. % + % (%); the admin guide's % + %.": 1,
    "semicolon \";\" in A stateful lifecycle is declared as a machine, its combinations with other machines are declared as a seam, and both declarations are checked in both directions: forwards by tests that drive the real transitions and crash them mid-flight, backwards by reading the stored data and proving it fits. This was retrofitted onto the payment machines across three large PRs (#2065, #2079, #2084); a new lifecycle declares it with its first slice, when it is cheap. The mechanisms, each with its reference:": 1,
    "semicolon \";\" in Declare the machine as data. Nodes carrying representative states built through the production transitions, events, and an exhaustive moves table — a cell missing from the table is a declared refusal, never a fallthrough. The framework is %; the machines are %, with whole-graph properties (all reachable, all can end, one declared provider-wait) in their % suites over the shared % walker.": 1,
    "semicolon \";\" in Enumerate flows × crash windows. List every flow; every await between two durable writes is a window; each window gets an idempotent-redelivery proof or a fault-injected test. Faults are triggers at SQLite's own boundary, not stubs (%), so the failing write rolls back exactly as production would.": 2,
    "banned-modal \"would\" in Enumerate flows × crash windows. List every flow; every await between two durable writes is a window; each window gets an idempotent-redelivery proof or a fault-injected test. Faults are triggers at SQLite's own boundary, not stubs (%), so the failing write rolls back exactly as production would.": 1,
    "semicolon \";\" in % — a complete allocation algorithm with zero imports; the hardest logic in the money paths and the easiest to test.": 1,
    "semicolon \";\" in % — pure projections over a slice of transfers; every derived total reuses the single % fold, so no two totals can disagree.": 1,
    "semicolon \";\" in One concept per file; one layer per directory. % defines where things go (% routes, % domain logic, % presentation). Within %, the shapes to copy:": 1,
    "semicolon \";\" in The filename states the concept; the concept fills the file. %, %, %, %, %, % — you can guess each file's exports from its name and vice versa. Function names carry contracts the same way: the % suffix on %/% means %, and % names the exact narrow column set it selects. If you cannot name the file in a couple of words, it is probably two concepts — split it.": 1,
    "semicolon \";\" in Validation is valibot; collections are % (via %); paths, media types, and cookies are %, %, and %; date/timezone math is % (temporal-polyfill); formatting is %. Valibot patterns to copy:": 4,
    "semicolon \";\" in Coercing schema factory — %: % validates digits before % (closing the % hole); % and friends are its specializations.": 1,
    "semicolon \";\" in Boundary validation — %: % an envelope % immediately after %, 400 on failure. Validate at the boundary; pass typed values inward.": 1,
    "semicolon \";\" in Before writing an algorithm, formatter, or parser, check % — the answer is usually already a dependency. When the project's calling convention differs from a library's, write a thin adapter; do not re-implement:": 1,
    "semicolon \";\" in % — all DST/offset math is %; % gets currency symbols and decimal places from % instead of a hand-maintained table; % validates with %.": 2,
    "semicolon \";\" in Nothing heavy at module load. Entry points only register the handler (%); app boot runs % on the first request (%). Module-load work is fine only when pure and cheap (for example % building its schemas once).": 1,
    "semicolon \";\" in Request-scoped memoization, not global state. % (%) shares one fetch among all callers within a request. Any new per-request state is built on one of the three factories in % (%, %, %) — the only module allowed to touch % — so two concurrent requests on one isolate cannot clobber each other and a leaked post-request context always reads as %. Isolate-lived caches are best-effort and bounded (%; the settings version-stamp cache in %) — never authoritative for security decisions, and invalidated automatically by the write-sniffing db client (%).": 1,
    "semicolon \";\" in Respect the subrequest budget. Fixed-cost designs like % (one SELECT plus one batch regardless of batch size), % instead of update-then-select (%), and % for multi-read round-trips. Bunny has a hard limit of 50 subrequests per request. One libsql %, batch, transaction begin/statement/commit/rollback, or external fetch counts as one; statements inside one batch still count as one. The client guard blocks database call 51, but routes that also call providers or storage must target at most 40 database calls so those other fetches still fit.": 1,
    "semicolon \";\" in Model realistic database latency. The request benchmark uses 0, 5, 10, and 20 ms per libsql round trip. Treat 20 ms as the expected worst case for a replicated database; do not publish 50 or 100 ms projections as realistic production measurements without evidence from production.": 1,
    "semicolon \";\" in One-round-trip patterns. % in %; %/% in %; keyset pagination for unbounded reads (% with %).": 2,
    "semicolon \";\" in Blind HMAC indexes for lookups. Alongside each searchable encrypted value sits a deterministic % index column: % (%), % (%, %), % for inbound SMS (%), % on modifiers. Query %; never scan-and-decrypt. (The one sanctioned scan-decrypt — invite codes in % — is documented and bounded by a tiny keyspace.)": 1,
    "semicolon \";\" in One blob, one decrypt, decrypt late. All attendee PII lives in a single % (%); list queries select it without decrypting (% and friends in %), and % runs only at render time. %'s % flag even gates which fields come out of the blob, and % decrypts just the name.": 1,
    "semicolon \";\" in Declarative encryption at the column layer. %/% in % decrypt lazily, per present column; a new encrypted column is declared, not hand-wired.": 1
  },
  "docs/env-vars.md": {
    "semicolon \";\" in The optional static CDN is different: %, %, %, %, and % are GitHub repository secrets used only while building. When all five are set, the build uploads site-independent browser assets and image-codec WASM under an immutable content-addressed path, purges the pull zone with the existing % repository secret, verifies every public object byte-for-byte, then bakes those public URLs and their CSP origin into the edge script. They must not be added to the running Bunny script. With all five absent, assets stay embedded; a partial set fails the build. Site-bound assets such as % and the dynamic % body remain in each script. Use the Storage API hostname shown on Bunny's Storage Access page for % (for example, % or %).": 1,
    "semicolon \";\" in % - Rows read per keyset page when dumping a table for backup (default 500). Each page is one libsql response, so this bounds the response size to stay under libsqld's % payload cap. Used by % and the admin Backups page; migrations no longer back up inline (the edge subrequest budget cannot fit a full dump), so backups are taken out-of-band.": 1,
    "semicolon \";\" in % - Shared secret authorizing the inter-instance site-credentials endpoint (%). When set on a builder/main instance, that endpoint returns built sites' DB URL + token to a caller presenting this key as a bearer token, so the upgrade workflow can back each site up to the builder's storage before deploying. The returned token is each site's own full-access credential (the same one the site runs with) — callers only read, but must treat the response as write-capable production secrets. The caller passes the release tier it is publishing as % (a tier-less call defaults to % ⇒ the whole fleet, which is what the single-site % action relies on); each site carries an % channel and only the sites at that tier or more eager are returned (a % deploy reaches every site, % reaches beta + alpha sites, % only alpha sites — an unknown tier is a 400). The response echoes the applied % so a caller can confirm the server actually filtered: a pre-tier build ignores the query string and omits it, letting the canary workflow fail closed instead of fanning a non-release deploy out to the whole fleet. Unset % ⇒ the endpoint is disabled (404). The upgrade workflow receives the key as a run-time input, not a stored GitHub secret.": 1,
    "participle \", letting\" in % - Shared secret authorizing the inter-instance site-credentials endpoint (%). When set on a builder/main instance, that endpoint returns built sites' DB URL + token to a caller presenting this key as a bearer token, so the upgrade workflow can back each site up to the builder's storage before deploying. The returned token is each site's own full-access credential (the same one the site runs with) — callers only read, but must treat the response as write-capable production secrets. The caller passes the release tier it is publishing as % (a tier-less call defaults to % ⇒ the whole fleet, which is what the single-site % action relies on); each site carries an % channel and only the sites at that tier or more eager are returned (a % deploy reaches every site, % reaches beta + alpha sites, % only alpha sites — an unknown tier is a 400). The response echoes the applied % so a caller can confirm the server actually filtered: a pre-tier build ignores the query string and omits it, letting the canary workflow fail closed instead of fanning a non-release deploy out to the whole fleet. Unset % ⇒ the endpoint is disabled (404). The upgrade workflow receives the key as a run-time input, not a stored GitHub secret.": 1,
    "semicolon \";\" in % - Sentry DSN for server-side error reporting (for example a self-hosted Bugsink: %). When set, the same classified server errors that log to the console and ping ntfy are also captured by Sentry, with a real stack trace when the originating exception is available. Unset ⇒ Sentry is disabled (the SDK never initializes). The release is %, matching the source maps the deploy workflows upload; readable (un-minified) traces additionally require the %, % (the instance base URL, for example %), %, and % GitHub Actions secrets so the deploy can inject debug IDs and upload the maps. Without those secrets the deploy still works; traces just stay minified.": 2,
    "semicolon \";\" in % - Optional diagnostic key. % returns a plain % by default; a request with a matching % header instead returns JSON build diagnostics (commit, build timestamp, server time) — non-private but useful to operators. Unset ⇒ verbose health disabled. The running build also records its commit into % on boot, so a backup carries the commit the site was on and a restore can surface which commit to redeploy (via %).": 1,
    "semicolon \";\" in % - Optional Botpoison public key (sent to the browser). The contact form works without it; setting it together with % adds proof-of-work spam protection as a progressive enhancement. The owner still enables the form under Site → Contact and sets a business email.": 1
  },
  "docs/external-order-library.md": {
    "participle \", using\" in The preview's Continue button sends the visitor to the canonical ticket page with the selected listings and quantities pre-filled, using the same booking flow as the existing % page.": 1,
    "banned-modal \"should\" in Then they mark links that should add listings:": 1,
    "banned-modal \"should\" in The % remains the no-JS fallback and should match %. The library treats % as the authoritative listing URL when both are present.": 1,
    "banned-modal \"would\" in This is a privacy boundary, not just a feature flag. A site can run with the Site feature off, in which case its listing slugs and names are effectively private — reachable only by someone who already holds the direct ticket URL. The embedded catalog would publish that whole list. So the order library must not be implied by any other setting; it has to be turned on explicitly, a deliberate % decision, independent of the Site feature, %, and %.": 1,
    "semicolon \";\" in This is a privacy boundary, not just a feature flag. A site can run with the Site feature off, in which case its listing slugs and names are effectively private — reachable only by someone who already holds the direct ticket URL. The embedded catalog would publish that whole list. So the order library must not be implied by any other setting; it has to be turned on explicitly, a deliberate % decision, independent of the Site feature, %, and %.": 1,
    "banned-modal \"would\" in The owner adds %, and that matters for enforcement. Cross-origin classic scripts execute without a CORS check — that is how CDN % works — which would let any site load the widget regardless of %. A cross-origin module script is CORS-checked, so when the server omits % for a disallowed origin the browser refuses to evaluate it. The module form is what makes the allowlist actually bite. Keep it ESM (%); do not copy the existing IIFE client bundles in % (which is how % is built today — %).": 1,
    "semicolon \";\" in The owner adds %, and that matters for enforcement. Cross-origin classic scripts execute without a CORS check — that is how CDN % works — which would let any site load the widget regardless of %. A cross-origin module script is CORS-checked, so when the server omits % for a disallowed origin the browser refuses to evaluate it. The module form is what makes the allowlist actually bite. Keep it ESM (%); do not copy the existing IIFE client bundles in % (which is how % is built today — %).": 1,
    "banned-modal \"would\" in But the owner's tag is not a security boundary. A disallowed site can ignore % and include the same URL as a classic %, which is not CORS-gated and would run the widget anyway. Two requirements close this:": 1,
    "banned-modal \"could\" in Treat the catalog as public regardless. Even with (1), a determined attacker could read the response body off the network; the only thing CORS + module syntax actually prevents is unauthorized widget execution/embedding, not disclosure. That is fine because the catalog is already-public listing data (see Security And Privacy) — but do not put anything in the module body that isn't already public.": 1,
    "semicolon \";\" in Treat the catalog as public regardless. Even with (1), a determined attacker could read the response body off the network; the only thing CORS + module syntax actually prevents is unauthorized widget execution/embedding, not disclosure. That is fine because the catalog is already-public listing data (see Security And Privacy) — but do not put anything in the module body that isn't already public.": 1,
    "contraction \"isn't\" in Treat the catalog as public regardless. Even with (1), a determined attacker could read the response body off the network; the only thing CORS + module syntax actually prevents is unauthorized widget execution/embedding, not disclosure. That is fine because the catalog is already-public listing data (see Security And Privacy) — but do not put anything in the module body that isn't already public.": 1,
    "banned-modal \"would\" in % runs before the database is initialized and before the request pipeline loads route settings (%). A pre-settings static route would only ever see default or stale %. % must be served from a handler that runs after settings load.": 1,
    "semicolon \";\" in % returns % (the whole path, since there is no second slash). Register that prefix in %; an unlisted prefix falls back to % and would decrypt the full snapshot just to read %. Scope the entry to only what the module needs: % (enable flag + embed allowlist + currency for price formatting). No payment, email, wallet, or SMS secrets are read on this path.": 1,
    "banned-modal \"would\" in % returns % (the whole path, since there is no second slash). Register that prefix in %; an unlisted prefix falls back to % and would decrypt the full snapshot just to read %. Scope the entry to only what the module needs: % (enable flag + embed allowlist + currency for price formatting). No payment, email, wallet, or SMS secrets are read on this path.": 1,
    "semicolon \";\" in The dynamic handler composes the response by prepending the serialized catalog to that bundled body (or substituting a placeholder token). The owner-facing logic stays in the bundle; only the catalog JSON varies per request.": 1,
    "semicolon \";\" in Include the static body in edge asset inlining and cache-busting; the catalog is injected at request time.": 1,
    "contraction \"isn't\" in At serve time the handler reads the site's non-hidden listings — % — and serializes a small catalog into the module. This is one step broader than the % and public listings pages, which use % (%, %): we also include closed (inactive) listings so the widget can tell a visitor a listing isn't bookable rather than silently sending them to the ticket page (see Browser Behaviour). Hidden listings are still excluded.": 1,
    "contraction \"don't\" in % reads the process-wide listings cache (%, %) — the same cache the public % and % pages already populate, so % adds no decryption those pages don't already do. Note % and % are themselves encrypted columns, so the catalog's own fields must be decrypted regardless; the catalog simply does not read the other encrypted fields (description, location, date, etc.).": 1,
    "semicolon \";\" in % reads the process-wide listings cache (%, %) — the same cache the public % and % pages already populate, so % adds no decryption those pages don't already do. Note % and % are themselves encrypted columns, so the catalog's own fields must be decrypted regardless; the catalog simply does not read the other encrypted fields (description, location, date, etc.).": 1,
    "wordy \"simply\" in % reads the process-wide listings cache (%, %) — the same cache the public % and % pages already populate, so % adds no decryption those pages don't already do. Note % and % are themselves encrypted columns, so the catalog's own fields must be decrypted regardless; the catalog simply does not read the other encrypted fields (description, location, date, etc.).": 1,
    "participle \", using\" in The client formats all displayed money — unit price, line totals, and the subtotal — itself, using % and %. It must mirror the server's % (%) so output matches the canonical pages: convert %, then %. This is a single % call mirroring one helper, not a reimplementation of checkout math. (An earlier draft forbade client formatting, which made % line totals unrenderable — that rule is dropped.)": 1,
    "contraction \"can't\" in % is % when the listing requires an input the v1 widget cannot supply, so a final price can't be shown. The conditions are direct % fields, each confirmed against the booking form:": 1,
    "semicolon \";\" in % — pay-what-you-want; % is the minimum, shown as a % price. For these the cart shows % and excludes them from the indicative subtotal. Answer-priced questions (an answer carrying a price %) are deliberately not treated as variable. Detecting them needs the question/answer/modifier graph for every listing — including assign-all questions, which have no per-listing % rows — on every public module fetch. Since the subtotal is already an explicit lower-bound estimate (modifiers and fees are excluded), such a listing shows its base % as a % price and the checkout caveat covers the rest. This is a v1 scope choice, not an oversight.": 1,
    "semicolon \";\" in Every entry carries % (= the listing's % flag). Bookable entries also carry % and %; closed (%) entries carry only % and % — enough to intercept the click with a % notice (see Browser Behaviour). Their price fields are omitted. % reflects only the stable owner % toggle. Time-window (%) and capacity (sold-out) states are not evaluated when building the catalog — like the indicative subtotal, those availability checks are resolved authoritatively at the ticket page. So a listing that is % but past its registration window still shows as bookable in the widget and the visitor learns it's closed at checkout. (Intercepting % covers the owner's explicit % case; intercepting time/capacity would mean evaluating % per listing on every module fetch, which v1 trades away for the same reason it shows an indicative rather than authoritative total.)": 2,
    "contraction \"it's\" in Every entry carries % (= the listing's % flag). Bookable entries also carry % and %; closed (%) entries carry only % and % — enough to intercept the click with a % notice (see Browser Behaviour). Their price fields are omitted. % reflects only the stable owner % toggle. Time-window (%) and capacity (sold-out) states are not evaluated when building the catalog — like the indicative subtotal, those availability checks are resolved authoritatively at the ticket page. So a listing that is % but past its registration window still shows as bookable in the widget and the visitor learns it's closed at checkout. (Intercepting % covers the owner's explicit % case; intercepting time/capacity would mean evaluating % per listing on every module fetch, which v1 trades away for the same reason it shows an indicative rather than authoritative total.)": 1,
    "banned-modal \"would\" in Every entry carries % (= the listing's % flag). Bookable entries also carry % and %; closed (%) entries carry only % and % — enough to intercept the click with a % notice (see Browser Behaviour). Their price fields are omitted. % reflects only the stable owner % toggle. Time-window (%) and capacity (sold-out) states are not evaluated when building the catalog — like the indicative subtotal, those availability checks are resolved authoritatively at the ticket page. So a listing that is % but past its registration window still shows as bookable in the widget and the visitor learns it's closed at checkout. (Intercepting % covers the owner's explicit % case; intercepting time/capacity would mean evaluating % per listing on every module fetch, which v1 trades away for the same reason it shows an indicative rather than authoritative total.)": 1,
    "semicolon \";\" in All non-hidden listings are included; hidden/unlisted listings are omitted for privacy (see Security And Privacy). A % to a hidden or unknown listing isn't enhanced and falls back to its plain %.": 1,
    "contraction \"isn't\" in All non-hidden listings are included; hidden/unlisted listings are omitted for privacy (see Security And Privacy). A % to a hidden or unknown listing isn't enhanced and falls back to its plain %.": 1,
    "semicolon \";\" in % is informational. The catalog can go stale between page loads; that is accepted (listings change slowly and a page refresh re-fetches the module). It is not a signed token and the module never refetches to refresh it.": 1,
    "semicolon \";\" in The catalog is identical for every origin; the only per-origin variance in the response is the CORS header.": 1,
    "banned-modal \"could\" in % and a non-cacheable % are required: the response varies by request % and embeds a catalog that depends on mutable settings, so a cached copy could serve a stale % after the owner tightens the allowlist. (A short % is acceptable in place of % if a brief staleness window is fine; the immutable static-asset cache is not.) The security stakes are lower than for the old API design — no private data is gated, the module only enhances links to already-public pages — but keeping the allowlist responsive is still worthwhile.": 1,
    "semicolon \";\" in % and a non-cacheable % are required: the response varies by request % and embeds a catalog that depends on mutable settings, so a cached copy could serve a stale % after the owner tightens the allowlist. (A short % is acceptable in place of % if a brief staleness window is fine; the immutable static-asset cache is not.) The security stakes are lower than for the old API design — no private data is gated, the module only enhances links to already-public pages — but keeping the allowlist responsive is still worthwhile.": 1,
    "wordy \"simply\" in Links that do not resolve to a catalog slug are left alone — including hidden/unlisted listings, which are intentionally absent from the catalog and so simply keep their no-JS %. In development builds the module may log a console warning; production should avoid noisy logs on owner sites.": 1,
    "banned-modal \"may\" in Links that do not resolve to a catalog slug are left alone — including hidden/unlisted listings, which are intentionally absent from the catalog and so simply keep their no-JS %. In development builds the module may log a console warning; production should avoid noisy logs on owner sites.": 1,
    "semicolon \";\" in Links that do not resolve to a catalog slug are left alone — including hidden/unlisted listings, which are intentionally absent from the catalog and so simply keep their no-JS %. In development builds the module may log a console warning; production should avoid noisy logs on owner sites.": 1,
    "banned-modal \"should\" in Links that do not resolve to a catalog slug are left alone — including hidden/unlisted listings, which are intentionally absent from the catalog and so simply keep their no-JS %. In development builds the module may log a console warning; production should avoid noisy logs on owner sites.": 1,
    "wordy \"e.g.\" in If the catalog entry is not bookable (closed), show a brief accessible notice such as % and stop — do not add to the cart and do not navigate. There is no point sending the visitor to the ticket page only to learn the same thing. (Live availability, e.g. sold-out, is still resolved at checkout; only the stable closed/% state is intercepted here.)": 1,
    "semicolon \";\" in If the catalog entry is not bookable (closed), show a brief accessible notice such as % and stop — do not add to the cart and do not navigate. There is no point sending the visitor to the ticket page only to learn the same thing. (Live availability, e.g. sold-out, is still resolved at checkout; only the stable closed/% state is intercepted here.)": 1,
    "semicolon \";\" in Use Shadow DOM for the widget shell so owner CSS does not accidentally break the cart. Render the summary markup in the widget itself with a small copy of the existing % rules; do not reuse the full site stylesheet on the external page.": 1,
    "semicolon \";\" in Catalog text must never be rendered as HTML. The server's serialization escaping (see Embedded Catalog) only protects the embedded JS/JSON; once the widget renders rows in the browser, a listing % like % would execute if inserted via %. The widget must insert all catalog-derived text (names, messages) with % or DOM node creation — never %/% with catalog data — so owner-provided names cannot run script on the external page. This is covered by a client test.": 1,
    "banned-modal \"would\" in Catalog text must never be rendered as HTML. The server's serialization escaping (see Embedded Catalog) only protects the embedded JS/JSON; once the widget renders rows in the browser, a listing % like % would execute if inserted via %. The widget must insert all catalog-derived text (names, messages) with % or DOM node creation — never %/% with catalog data — so owner-provided names cannot run script on the external page. This is covered by a client test.": 1,
    "semicolon \";\" in Variable-price listings (%, customisable days, or pay-what-you-want) show % and are excluded from the subtotal. (Answer-priced questions are not flagged — see the Embedded Catalog note; their base price shows as a % estimate under the same caveat.)": 1,
    "wordy \"e.g.\" in The subtotal is labelled to set expectations, e.g.:": 1,
    "semicolon \";\" in This deliberately avoids replicating the booking engine (tiers, fees, rounding, sold-out logic) in JavaScript. The single source of truth for the authoritative total is the canonical ticket page, which the visitor reaches via Continue. The widget only formats raw unit prices; matching the canonical display is a matter of mirroring one % call (%, %), not reproducing pricing logic. Quantity/tier modifiers and the booking fee are intentionally not applied here — the subtotal is a lower-bound estimate.": 1,
    "wordy \"simply\" in If the module is blocked (disallowed origin / CORS), it never evaluates, so the marked links simply work as ordinary links.": 1,
    "wordy \"simply\" in Hidden/unlisted listings are never embedded, so the module cannot be used to enumerate them. A % pointing at a hidden listing is simply not enhanced; its % still opens the real ticket page (the listing remains reachable by its exact URL, exactly as today). This preserves the progressive-enhancement promise without making unlisted listings discoverable.": 1,
    "semicolon \";\" in Hidden/unlisted listings are never embedded, so the module cannot be used to enumerate them. A % pointing at a hidden listing is simply not enhanced; its % still opens the real ticket page (the listing remains reachable by its exact URL, exactly as today). This preserves the progressive-enhancement promise without making unlisted listings discoverable.": 1,
    "semicolon \";\" in No cookies are read or set; % holds slugs and quantities only.": 1,
    "semicolon \";\" in % is not a security token; do not treat module freshness as an authorization signal.": 1,
    "banned-modal \"may\" in Both may exist on the same external site. The iframe embed creates booking forms in place; the external order library enhances links and creates a floating cart. They should not share global state.": 1,
    "semicolon \";\" in Both may exist on the same external site. The iframe embed creates booking forms in place; the external order library enhances links and creates a floating cart. They should not share global state.": 1,
    "banned-modal \"should\" in Both may exist on the same external site. The iframe embed creates booking forms in place; the external order library enhances links and creates a floating cart. They should not share global state.": 1,
    "wordy \"e.g.\" in The served module body contains module-only syntax (e.g. %) so it throws if loaded as a classic script.": 1,
    "semicolon \";\" in Links are enhanced only when % resolves to a catalog slug at the module origin; unknown and hidden slugs are left as plain links.": 1,
    "participle \", keeping\" in Rename the admin setting copy from iframe-only embedding to external-site access, keeping the % storage key. Add a new boolean setting % (default %) with its own settings-page toggle.": 1,
    "semicolon \";\" in Add the dynamic % handler: runs after settings load; when % is off, returns the console-notice stub with %; when on, gates CORS on % with % and % and embeds the catalog. Register the % prefix in % as %.": 2,
    "semicolon \";\" in Build the embedded catalog via %; emit each entry's slug, name, and % (= %), plus id, minor-unit %, and % for bookable ones, and a top-level %/% descriptor (from % / %). % = %. Escape via %.": 1,
    "semicolon \";\" in Authoritative pricing (opt-in). If exact totals, fees, or live availability are ever needed inside the widget, add an opt-in server quote endpoint that reuses the % quote path. This was considered for v1 and dropped in favour of the AJAX-free embedded-catalog design; it remains the documented escape hatch if indicative pricing proves insufficient.": 1
  },
  "docs/group-page-corrections.md": {
    "semicolon \";\" in Extend the grouped booking reader only where a caller genuinely needs it; the balance suppression removes the balance success read this contract once planned. Keep listing IDs, quantities, and package IDs associated until disclosure is decided. Do not add package tags to synthetic balance metadata.": 1
  },
  "docs/mutation-testing.md": {
    "semicolon \";\" in It reports a mutation score and lists each survivor as %. Exit code is non-zero if any mutant survived, so it can gate CI on a chosen module. By default it runs the test files directly (fast, for pure-unit modules); pass % for tests that import the app / Stripe and need built static assets + stripe-mock. Under %, mutating a client-bundle source (anything bundled into % — for example % or a module it imports) rebuilds just the affected bundle for each mutant, so the mutation reaches the built asset the tests load. Likewise, a mutant in any file that feeds the run-wide prebuilt test state (the golden schema DB and captured setup ceremony — the import graph of %, see %) runs direct tests without the stale state. If the mutant survives, the runner builds one fresh state from the mutant and shares it across all integration-test batches.": 1,
    "semicolon \";\" in Static gates apply mutants in isolated sibling copies. Mutants that pass are written over the run's source file, tested in a fresh % subprocess, then restored. The normal % command first copies the current checkout (including dirty source/test edits, excluding %, cache/report folders, local databases, secrets, and generated assets) to %; test-stage writes and per-mutant bundle rebuilds happen inside that copy, not the live files. Static worker copies are deleted before tests start. A run deletes its main copy as soon as it ends — reporting the failure if it cannot — so % does not fill up with checkout copies. While a run is going — and until the next run starts — it has a small % holding the child PID/status, so a stray run is easy to find and stop. Starting a run clears out the folders of every earlier run that is no longer going, including any whose % is unreadable because it was killed mid-write:": 1,
    "semicolon \";\" in In-place mutation inside the copied checkout is what makes mutations bind through % import-map aliases. The operator tables and AST walk are vendored from Mutasaurus (MIT); its own execution model writes a temp copy but runs the original tests, so every mutant falsely % on an alias-based project — see %.": 1,
    "present-perfect \"has been\" in Before it runs the mapped tests, the runner puts every mutant through two cheap static gates, ordered cheapest-first: a per-file Biome lint and then a % type-check. Static gates run concurrently in isolated sibling copies, with a CPU-aware limit capped at four (% can lower it). One- and two-mutant files stay serial to avoid copy overhead. Test batches keep their separate % limit and still run only after static results are reported in mutant order. No mutant is ever judged by a clock: gates and tests run to completion, so a mutant is killed only when a gate rejects it or a test fails, and survives otherwise. A slow type-check or a long queue wait can no longer be mistaken for a mutant being caught. The one clock left is %, a whole-run guard (default one hour) against a mutant that hangs the tests: when it fires the run fails with no score and no summary, printing only how far it got and where to look. It fires the same way during the baseline, where nothing has been tested yet — a run the guard stopped is a failure to report, never an operator's interrupt. Keep the Biome calls one-shot unless a new benchmark proves otherwise: with pinned Biome 2.4.16, 20 warm one-file runs measured a 17.3 ms standalone median and a 51.2 ms % median. Either gate exiting non-zero kills the mutant without spending a full % on it — both a forbidden lint diagnostic and a type error are build failures, so the mutant can never ship, and static checks are far faster than the suite. The type-check gate catches the mutants that turn valid code into a type error — for example a % swap on a string concatenation (% does not type-check), or any operator change that violates a parameter/return type. Each gate is only trusted after the runner confirms the unmutated target passes it (the baseline probe): a standalone % does not run %/% first, so if the target is not already clean the run aborts loudly rather than scoring a bogus 100\\%. This means a mutant recorded in % must be one that survives both gates and the tests; a mutation that produces a type error never reaches the ignore-list because the type-check gate kills it first.": 1,
    "semicolon \";\" in Before it runs the mapped tests, the runner puts every mutant through two cheap static gates, ordered cheapest-first: a per-file Biome lint and then a % type-check. Static gates run concurrently in isolated sibling copies, with a CPU-aware limit capped at four (% can lower it). One- and two-mutant files stay serial to avoid copy overhead. Test batches keep their separate % limit and still run only after static results are reported in mutant order. No mutant is ever judged by a clock: gates and tests run to completion, so a mutant is killed only when a gate rejects it or a test fails, and survives otherwise. A slow type-check or a long queue wait can no longer be mistaken for a mutant being caught. The one clock left is %, a whole-run guard (default one hour) against a mutant that hangs the tests: when it fires the run fails with no score and no summary, printing only how far it got and where to look. It fires the same way during the baseline, where nothing has been tested yet — a run the guard stopped is a failure to report, never an operator's interrupt. Keep the Biome calls one-shot unless a new benchmark proves otherwise: with pinned Biome 2.4.16, 20 warm one-file runs measured a 17.3 ms standalone median and a 51.2 ms % median. Either gate exiting non-zero kills the mutant without spending a full % on it — both a forbidden lint diagnostic and a type error are build failures, so the mutant can never ship, and static checks are far faster than the suite. The type-check gate catches the mutants that turn valid code into a type error — for example a % swap on a string concatenation (% does not type-check), or any operator change that violates a parameter/return type. Each gate is only trusted after the runner confirms the unmutated target passes it (the baseline probe): a standalone % does not run %/% first, so if the target is not already clean the run aborts loudly rather than scoring a bogus 100\\%. This means a mutant recorded in % must be one that survives both gates and the tests; a mutation that produces a type error never reaches the ignore-list because the type-check gate kills it first.": 1,
    "semicolon \";\" in Known-equivalent survivors recorded in % are suppressed, as with a manual run. Never record %/% mutants: Biome's % rule is configured to reject loose comparisons even against %, and the runner counts that lint failure as killed before tests run. Use % to check the whole equivalent list with lint and type-check only; pass % to remove entries those static gates now kill. The audit never runs tests and refuses to rewrite stale or malformed entries. Like %, it works in a copy of the checkout under %, so the live source files are never left mutated and a commit made while it runs cannot pick up a mutant. With %, the pruned % registry files are copied back when the run ends — unless one was edited meanwhile, which fails the run instead of overwriting the edit.": 1
  },
  "docs/payment-aggregate-lessons.md": {
    "semicolon \";\" in The payment-aggregate rewrite (PRs #1962 and #1973) was closed without merging. Origin: % at the time it was retired; the file is deleted, and this document keeps the record. Before it closed, the branch went through repeated adversarial review and a coverage audit. The full record — around forty-five findings, each tied to a file and line on that branch — is in % at commit % on % (also reachable as %). The table-hardening slice is at % (%). None of that branch's code is on main, so the findings are not jobs here — they are the failure map for the redo that PLAN.md plans.": 1,
    "semicolon \";\" in Each of these was found, and most were confirmed, on the rewrite. They are worth reading in full at the commit above before re-attempting the aggregate; in one line each:": 1,
    "semicolon \";\" in One stuck item must never block the queue behind it: a saved owner decision the provider kept refusing halted every checkout, completion and refund; one unredactable payment halted all payment-history redaction and the pruning behind it.": 1,
    "banned-modal \"may\" in Deferred work must run from facts saved when the money moved, not from live rows an owner may have edited since — quantity, currency, recipient, and the booking itself all drifted between capture and completion.": 1,
    "banned-modal \"could\" in Money-adjacent actions must be exact about their targets: a % that could refund, a refund list that included charge-less payments, and a confirmed refund that never reached the ledger all moved or lost money on paper.": 1,
    "semicolon \";\" in Recorded so nobody re-raises them here: SumUp amounts convert once at checkout creation (no stored retry); emails send inline reading the config at send time (no queue to go stale); the attendee merge already fences live payment rows (%); Square's % is already optional in %; the success redirect already clears session tokens, and a revisit renders without them.": 4
  },
  "docs/precommit-status.md": {},
  "docs/scheduled-maintenance.md": {
    "banned-modal \"would\" in Rate-limit % at the CDN before requests reach the edge script. The application deliberately does not keep a request counter for rejected calls: doing so would let unauthenticated traffic consume database subrequests before authentication. Keep the limit high enough for the monitor's retries, but far below a general API traffic rate.": 1
  },
  "docs/test-duplication.md": {
    "semicolon \";\" in The merges, in the order they landed: % adopted at the four files that hand-rolled it; % widened to carry a result back and adopted at seven; % and % in %; % in %; %; the reader's presses, the erase form and Refund All curried; the named step types %, %, % adopted at 31 helpers and the new % at nine more; % moved out of %; %, %, %, %, %, %; and the named shapes %, %, %, % and %.": 9,
    "banned-modal \"could\" in The single pair at 48 tokens was the find that made the scan worth adding. % held its own copy of % and % from %. No config could see it, because no config scanned % against %. The golden test database was therefore built by a second copy of the production schema builder, and could drift from it. It now calls %.": 2
  },
  "e2e-payments/README.md": {
    "semicolon \";\" in a fresh file-backed app database;": 1,
    "semicolon \";\" in a new real % child process;": 1,
    "semicolon \";\" in a new tunnel for paid providers (or the local URL for free);": 1,
    "semicolon \";\" in one browser process with separate authenticated owner and cookie-free visitor browser contexts (a second owner context for the stale-form race);": 1,
    "semicolon \";\" in a unique run identity (booker email, listing names, owner credentials — never the defaults); and": 1,
    "semicolon \";\" in stripe-refund-recovers — Stripe returns money while the local Money write fails; the booking stays protected, a stale form cannot double-refund, and Refresh completes the local record when Money recovers.": 1,
    "semicolon \";\" in square-refund-safe — a Square payment is replayed and refunded; the refund is either recorded or safely observing, with no second Refund.": 1,
    "semicolon \";\" in sumup-refund-safe — a SumUp payment is replayed (genuine self-delivered callback); forged ids receive the one fixed refusal without a read; the keyless refund reaches a safe result.": 2,
    "semicolon \";\" in sumup-return-pending — the visitor opens the payment return before paying; the waiting page says the payment is not confirmed yet and keeps checking on its own, no error is logged, the timed window stops after ten checks with a click on Check again opening a fresh one, and the same return books once the payment is confirmed.": 1,
    "semicolon \";\" in stripe-invalidated-checkout-refunded — the owner changes the price while a visitor is paying; the webhook processes the later charge, retains the booking at quantity 0, and automatically refunds.": 1,
    "semicolon \";\" in The app is driven the way a person drives it: fill fields by their accessible name, click buttons and links by their visible text, and submit through the visible submit control (%) — never force, never %. Some headless Chromium builds stop scheduling compositor frames after a form POST, which stalls Playwright's stability wait; the harness detects that and falls back to % / a scripted click, which still runs browser validation and the app's own event handlers. A failed ordinary action is only replayed through that fallback when a page-side witness proves the click never dispatched — a post-dispatch failure rethrows, so a live refund form can never be submitted twice by the fallback.": 1
  },
  "scripts/email-sandbox-e2e/README.md": {
    "semicolon \";\" in % is optional; a failed run pings it, as the payment harness does.": 1
  },
  "scripts/unread-fields/README.md": {
    "banned-modal \"would\" in % and % are two fields. A static belongs to the class object, and the report calls that object %, which is what TypeScript calls it. One line for both would call a field on a value read because the class side is.": 1,
    "banned-modal \"would\" in A % block does not count either. It adds its shapes to the global scope rather than to what the file exports. The one in % holds the JSX contract, and the compiler reads those fields rather than any code here, so the scan would call them unread.": 1,
    "wordy \"simply\" in A shape also hands on the fields it takes from somewhere else. That means a base it extends, an intersection, another type it simply names, or every arm of a union. % names one type, and % names two. The scan looks at the fields of every arm. A reader reaches the fields of one arm, after it narrows the value to that arm. The arms are often types the file keeps to itself, so no other shape reports them. A field written down in a library, such as the % that % carries, is not a field this repository exports.": 1
  }
}
