# Known-equivalent mutants — see README.txt in this directory.

# buildAssetPathsModule embed-script gate `pathConst === "EMBED_JS_PATH"`:
# pathConst is the string 4th element of an AssetDef tuple compared against a
# string literal, so === and == never disagree — no null/undefined/number
# operand exists to coerce.
# The generated WASM modules terminate every statement with punctuation, so
# joining their source lines with "" or ";" parses and behaves identically.
scripts/inline-jsquash-wasm.ts::buildModule~0igalf0  ; → ""   # inline module remains semicolon/brace delimited
scripts/inline-jsquash-wasm.ts::buildRemoteModule~0dmdxd1  ; → ""   # remote module remains semicolon/brace delimited

# bundle-composition/model.ts — SourceMapSegment is either the one-value
# unmapped tuple or a mapped tuple carrying both source index and original line,
# so indexes 1 and 2 are present or absent together.
scripts/bench/bundle-composition/model.ts::isMappedSegment~01f9dl9  1 → 2   # isMappedSegment: segment[1] and segment[2] are both defined exactly for mapped tuples

# scripts/pr-queue/summary.ts operands can never be falsy-but-non-null, so
# `?? → ||` cannot be observed.
scripts/pr-queue/summary.ts::displayName~1h388k9  ?? → ||   # BOT_NAMES[login]: string|undefined, values are non-empty, so only falsy value is undefined === fallback path
scripts/pr-queue/summary.ts::aggregateUnresolved.entry~1imymi1  ?? → ||   # by.get(author): entry object or undefined; an object is always truthy, so ?? and || agree

# scripts/pr-queue/render.ts — Object.groupBy leaves a bucket key undefined when
# empty and a non-empty array otherwise, so neither `??` operand is ever
# falsy-but-non-null (an empty group has no key, never a length-0 array).
scripts/pr-queue/render.ts::renderReport.counts~1lcxgnr  ?? → ||   # byBucket[b]?.length: number|undefined; a present group has length>=1, so only falsy value is undefined === fallback 0
scripts/pr-queue/render.ts::renderReport.body.group~0wgnloo  ?? → ||   # byBucket[b]: array|undefined; an array is always truthy, so ?? and || agree

# scripts/pr-queue/pagination.ts — the rollup contexts connection is an object
# or (via optional chaining) undefined; an object is always truthy, so the
# `?? null` fallback and `|| null` agree on every value it can hold.
scripts/pr-queue/pagination.ts::latestChecks~1qbpvum  ?? → ||   # statusCheckRollup?.contexts: object|undefined; object is truthy, so ?? and || agree

# scripts/pr-queue/summary.ts — hasTruncatedConnection's `<boolean|undefined> ?? false`:
# for a boolean the only falsy-non-null value is `false`, and `false ?? false`
# === `false || false` === false, so ?? and || agree on every value.
scripts/pr-queue/summary.ts::hasTruncatedConnection~1uwoobj  ?? → ||   # hasNextPage is boolean|undefined; false ?? false === false || false, so ?? and || agree

# Unit-tests-report import attribution (unit-tests-report-imports.ts) — the
# stem() dir-strip replacement value is unobservable.
scripts/unit-tests-report-imports.ts::stem~0mxx1lm   → "mutated"   # stem()'s first replace strips the directory before every `===` comparison in findMisplacedTests; both sides always have a leading `dir/` (tests live under test/, sources under src/), so the replacement string is prepended to both operands identically — stem(test) === stem(source) is invariant under its value

# Test-tree scan: the subject lookup's default for an unselected path.
scripts/test-tree-scan.ts::scanTestTree.subjectsOf~0mczjth  ?? → ||   # subjects.get() yields either an array (always truthy, even when empty) or undefined, so ?? and || pick the same fallback

# Mutation test map: null-coalescing and the integration split.
scripts/mutation/test-map.ts::mirrorOwnerOrNull~0tdqf2c  ?? → ||   # the value is a mirror prefix taken from testPrefix(), which always yields a non-empty path string, so the only falsy value it can take is the undefined of an empty match — where ?? and || agree
scripts/mutation/test-map.ts::buildMutationTestMap.integrationTestFiles~12ldm3l  && → ||   # ownedTest gives every integration test owner null and every other selected test a non-null owner (an owner-less non-integration test throws above), so `owner === null` and `isIntegrationTest(testFile)` are always equal here and the two operators agree

# Bundle AST bench: the parser's filename argument is a diagnostic label only.
scripts/bench/bundle-composition/javascript-ast.ts::countJavaScriptAstNodes.result~0qowwnh  bundle.js → ""   # oxc uses the name only to label diagnostics and to pick a language from its extension; "bundle.js" and "" both select the default JavaScript parser, and the only parser output this module reads is errors[0].message (which never contains the name) and the node count

# Script cleanup: the second length check can never see a single error.
scripts/cleanup.ts::throwCollectedErrors~0ppty15  1 → 0   # the line above already throws when there is exactly one error, so this check only ever runs with 0 or 2+ errors, where > 1 and > 0 agree

# Compact test reporter: fallbacks for values the regexes always provide, and
# comparisons an earlier line has already ruled out.
scripts/compact-test-reporter.ts::leadingWhitespaceLength~16fiu5f  ?? → ||   # /^\s*/ matches every string, so the optional chain always yields a length and the fallback never runs
scripts/compact-test-reporter.ts::leadingWhitespaceLength~16fiu5f  0 → 1   # same line: the fallback is unreachable because /^\s*/ always matches
scripts/compact-test-reporter.ts::stripCommonIndent.indent~0fo5ov5  0 → 1   # this arm runs only when every line is blank, and slicing 0 or 1 characters off blank lines leaves the same text once the join is trimmed
scripts/compact-test-reporter.ts::parseYamlMessage.value~0ey8tn6  ?? → ||   # match[2] exists whenever the line matched, and its trimmed value is a string, so ?? "" and || "" agree
scripts/compact-test-reporter.ts::parseYamlMessage.value~0ey8tn6   → "mutated"   # unreachable fallback: match[2] is always present on a matched line
scripts/compact-test-reporter.ts::parseYamlMessage.baseIndent~1qh3i1b  ?? → ||   # match[1] is the indent capture, always present on a matched line
scripts/compact-test-reporter.ts::parseYamlMessage.baseIndent~1qh3i1b   → "mutated"   # unreachable fallback for the same reason
scripts/compact-test-reporter.ts::parseYamlAt.atIndent~0prxq5p  ?? → ||   # atIndex came from findIndex, so lines[atIndex] is always a string
scripts/compact-test-reporter.ts::parseYamlAt.atIndent~0prxq5p   → "mutated"   # unreachable fallback for the same reason
scripts/compact-test-reporter.ts::parseYamlAt~1qh3i1b  ?? → ||   # match[1] is one of file/line/column on a matched line, never missing
scripts/compact-test-reporter.ts::parseYamlAt~1qh3i1b   → "mutated"   # unreachable fallback; an unknown key is ignored by assignAtField either way
scripts/compact-test-reporter.ts::parseYamlAt~0k4hces  ?? → ||   # match[2] is always present on a matched line
scripts/compact-test-reporter.ts::parseYamlAt~0k4hces   → "mutated"   # unreachable fallback for the same reason
scripts/compact-test-reporter.ts::parseTapDiagnosticBlock~1fk341p  ?? → ||   # parseYamlTapDiagnostic returns an object or undefined, so its only falsy value is the undefined the fallback handles
scripts/compact-test-reporter.ts::isStepFailureDiagnostic~11y2w9l  ?? → ||   # diagnostic.message is a string or undefined, and the empty string it can be is the fallback itself
scripts/compact-test-reporter.ts::isStepFailureDiagnostic~11y2w9l   → "mutated"   # neither the empty string nor "mutated" matches the step-failure pattern, so the outcome is the same
scripts/compact-test-reporter.ts::locationFromStack.matches~1g3iamw  ?? → ||   # String.match with a global pattern returns an array or null, and an array is always truthy
scripts/compact-test-reporter.ts::estimateTapEventCount.text~00yrpxr   → "mutated"   # the fallback text for an unreadable file is only counted for test declarations, and neither the empty string nor "mutated" holds one
scripts/compact-test-reporter.ts::CompactTapReporter.constructor.this%2e%23estimatedTotal~1n8f285  ?? → ||   # an estimated total of 0 falls back to 0, so ?? and || pick the same number
scripts/compact-test-reporter.ts::CompactTapReporter.constructor.this%2e%23estimatedTotal~1n8f285  0 → 1   # the total is immediately grown to the number of finished tests, which is 1 or more whenever it is read
scripts/compact-test-reporter.ts::CompactTapReporter.constructor.this%2e%23hideProgress~03zoewn  ?? → ||   # hideProgress is a boolean or undefined, and false falls back to false either way
scripts/compact-test-reporter.ts::CompactTapReporter.constructor.this%2e%23stdout~11exve5  ?? → ||   # the stdout option is a function when present, and functions are always truthy
scripts/compact-test-reporter.ts::CompactTapReporter.constructor.this%2e%23stderr~0il4lt8  ?? → ||   # the stderr option is a function when present, and functions are always truthy
scripts/compact-test-reporter.ts::CompactTapReporter.consumeLine~1dqn0ov@2  --- → ""   # a line that trims to nothing has already returned above, so comparing the trimmed line with "" is never true
scripts/compact-test-reporter.ts::CompactTapReporter.consumeLine~1w2cvti@2  ... → ""   # same line, same reason
scripts/compact-test-reporter.ts::CompactTapReporter.%23recordFailure.location~13av20m  ?? → ||   # locationFromStack returns a location object or undefined, and objects are always truthy

# Mutation summary: grouped results are arrays or absent, never falsy-but-set.
scripts/mutation/summary.ts::summarize.count~009kdgl  ?? → ||   # Object.groupBy gives each status a non-empty array or nothing at all, and an array is always truthy
scripts/mutation/summary.ts::summarize.survivors~1orwjuw  ?? → ||   # the survivors group is an array when present, so ?? [] and || [] pick the same list

# Lock file: the handle exists only to carry the advisory lock.
scripts/lock-file.ts::openLockFile.read~1kby7vv  true → false   # nothing ever reads or writes through this handle, so opening it write-only still takes and releases the same lock

# Process helpers: the timer handle before the timer exists.
scripts/process.ts::beforeTimeout.timeout~031tixz  0 → 1   # the promise body runs straight away and overwrites this handle, so its starting value is never the one cleared
scripts/process.ts::beforeTimeout.delayed.timeout~0g1xgyk  = → +=   # the handle starts at 0, so adding to it and replacing it store the same timer id

# Isolated mutation runs: the exit code before any path has set one.
scripts/mutation/isolation.ts::runInSnapshot.exitCode~1tg3w08  1 → 0   # every path overwrites this before it is returned: the claim always runs its callback, which either sets 130 on an interrupt or hands back a child whose status sets the code, and any throw is caught and sets it there
scripts/mutation/isolation.ts::runInSnapshot.copyBack~11e4190  ?? → ||   # run.copyBack is a list or undefined, and a list — empty included — is truthy, so only undefined reaches the fallback and both operators yield the same []
scripts/stale-claim.ts::startClaimTouching.scheduleNextTouch.timeout~1tjxi69  = → +=    # the uncancelled timer's one late touch bails on the stopped flag before any read or write
src/shared/db/migrations.ts::initDb~0hi1ix2  ?? → ||   # opts.allowMissingSettings is boolean | undefined, and false ?? false and false || false both yield false

# Cucumber runs: telling Cucumber to be strict, when we already reject the
# statuses strictness is about.
scripts/specs/run.ts::cucumberConfiguration.provided.strict~1w2j4s2  true → false   # strict only changes how Cucumber treats undefined and pending steps, and messageIssues rejects exactly those two statuses (REJECTED_STATUSES in scripts/specs/messages.ts), so a run containing either fails on the issues whatever strict says, and a run containing neither is unaffected by it
src/shared/crypto/encryption.ts::importKey~1usmfin  false → true   # importKey is module-private and the CryptoKey it returns never leaves this file, which only encrypts and decrypts with it — nothing can call exportKey on it, so extractable is unobservable

# Comment checks: initial values that the first pass always overwrites, and a
# scan starting on a character no comment can begin with.
scripts/check-comments/rules.ts::widestLine.offset~0a4sfpt  0 → 1   # a comment's first line is always at least "//" (2 chars), so it exceeds the starting width and assigns offset on the first pass — the initial value is never the one returned
scripts/check-comments/rules.ts::widestLine.width~1myoetc  0 → 1   # same first pass: no comment's widest line can be 1 column, so a start of 0 and a start of 1 are both replaced by the real width before it is read
scripts/check-comments/rules.ts::namesMentioned~1nnu9xx  ?? → ||   # String.match with a global regex returns null or a non-empty array, and every array is truthy, so only null reaches the fallback and both operators yield the same []
scripts/check-comments/rules.ts::findDeadLinks.line~1my9h0w  0 → 1   # the count runs from the comment's own start, and a comment always begins with "/", so skipping character 0 can never miss a newline
scripts/check-comments/rules.ts::namesMentioned~1ugmzzy  %20 → ""   # the link pattern captures identifier characters greedily, so the character after every match is always a non-identifier that already separates the tokens — replacing a link with a space and with nothing give the same names for every input

# Unread-fields scan: the tree walk's fallback, where the only falsy result is
# the one the coalesce already handles.
scripts/unread-fields/writes.ts::nodeAt.descend~1bsvjf5  ?? → ||   # ts.forEachChild returns either undefined or a node the callback gave it, and every node is a truthy object, so nothing falsy-but-defined can reach the fallback and both operators pick the same value
