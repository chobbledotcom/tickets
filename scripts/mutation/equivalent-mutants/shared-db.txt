# Known-equivalent mutants — see README.txt in this directory.

# Table inputs make these operators equivalent: nullable query results exclude
# undefined, paired null checks already reject both nullish values, and optional
# conditions/dependencies are objects or arrays.
src/shared/db/table.ts::defineTable.updateStatement.args~1p65ca8  ?? → ||   # condition?.args is an array or undefined, and every array is truthy
src/shared/db/table.ts::cachedTable~1o37jut  ?? → ||   # dependsOn is a readonly array or undefined, and every array is truthy
src/shared/db/table.ts::defineCachedListTable.dependsOn~1o37jut  ?? → ||   # the cached table's dependsOn is the same array-or-undefined as above

# Login attempt rows store an integer count. For undefined, null, zero, or any
# non-zero integer, `(attempts ?? 0) + 1` and `(attempts || 0) + 1` agree.
src/shared/db/users.ts::getUserAuthFieldsById~0q9twz3  ?? → ||   # queryAll()[0] is a truthy UserAuthFields object or undefined, so both operators return the row or null
src/shared/db/query-log.ts::getState~0iubkry  ?? → ||   # queryLogScope.current() is a truthy QueryLogState object or undefined
src/shared/db/query-log.ts::enforceN1Guard.count~1xuztjh  ?? → ||   # stored read counts start at 1 and remain positive, so only undefined reaches the zero fallback
src/shared/db/query-log.ts::trackSql.state~0ab0ecr  ?? → ||   # store is a truthy QueryLogState object or undefined
src/shared/update.ts::recordScriptVersion.changes~148v29c  ?? → ||   # the stored commit is a string or undefined and the fallback is empty, so both operators return the same string
src/shared/update.ts::readRecordedScriptCommit~0csc5ea  ?? → ||   # the primary row value is a string or absent and the fallback is empty, so both operators return the same string

# Attendee phone blind-index (attendee-phone-index.ts).
src/shared/db/attendee-phone-index.ts::findAttendeeIdByPhoneIndex~0yjm3h8  ?? → ||   # row?.id is undefined or an AUTOINCREMENT id (≥1, never 0), so ?? and || agree

# Attendee group-capacity lookups: Map values are arrays (always truthy), and
# duration normalization clamps both a default of 0 and 1 to one day.
src/shared/db/attendees/capacity/groups.ts::getGroupPerDayRemaining.loads~1nauhgk  ?? → ||   # rowsByGroup.get(): GroupRow[]|undefined — an array is always truthy
src/shared/db/attendees/capacity/snapshot.ts::loadsByListing~0wzfdt0  ?? → ||   # byListing.get(): row[]|undefined — an array is always truthy, so both operators reach perDayLoads with the same rows
src/shared/db/attendees/capacity/snapshot.ts::loadCapacitySnapshot.membership~0tc0opa  ?? → ||   # knownMembership is a Map or undefined; every Map, including an empty one, is truthy
src/shared/db/attendees/capacity/remaining.ts::getListingRemainingForRange.durationDays~18guaiu  1 → 0   # daysOfSpan clamps the span back to one day, so a default of 0 reads the same single day

# Listing relationship day prices are stored as plain objects. Existing objects
# are truthy, while a missing Map entry is undefined, so `?? {}` and `|| {}` agree.
src/shared/db/listing-relationship-validation.ts::listingById.day_prices~1ag3qoh  ?? → ||   # Map values are DayPrices objects (always truthy); a missing entry is undefined

# Group-cap sweep (attendees/update.ts): the boundary-delta accumulator is a
# Map<string, number>; the only falsy-but-non-null number is 0 and the fallback
# is 0, so `?? 0` and `|| 0` always agree.
src/shared/db/attendees/update.ts::loadsAtBoundaries.deltas~0b895vw  ?? → ||   # Map<string,number>.get delta; 0 ?? 0 === 0 || 0
src/shared/db/attendees/update.ts::loadsAtBoundaries.deltas~1kwldm9  ?? → ||   # Map<string,number>.get delta; 0 ?? 0 === 0 || 0

# attendees/tokens.ts — `?? → ||` sites where the operand is a Map#get() whose
# value is always a truthy object or array (never a falsy-but-non-null value),
# so ?? and || agree for every possible value.
src/shared/db/attendees/tokens.ts::resultsInTokenOrder.tokenToResult~17m7xgj  ?? → ||   # resultsInTokenOrder: byTokenIndex.get() is Result|undefined; Result is AttendeeWithBookings or AttendeeBookingRows (always a truthy object), so ?? and || agree
src/shared/db/attendees/tokens.ts::resultsInTokenOrder~0uhrwgz  ?? → ||   # resultsInTokenOrder: tokenToResult.get() is Result|undefined; Result is always a truthy object (see 159), so ?? and || agree
src/shared/db/attendees/tokens.ts::tokenResultMap~1c5bf54  ?? → ||   # tokenResultMap: bookingsByAttendee.get() is Booking[]|undefined; an array is always truthy (even empty), so ?? and || agree

# db/capacity.ts — duration defaults that clampDurationDays clamps: a
# `durationDays` of 0 (the mutant) and 1 (the default) both normalize to a
# one-day span, so no input can tell the two apart.
src/shared/db/capacity.ts::dateToRange.durationDays~18guaiu  1 → 0   # dateToRange's default duration; clampDurationDays clamps 0 up to 1
src/shared/db/capacity.ts::buildCapacityCondition.durationDays~18guaiu  1 → 0   # buildCapacityCondition's default duration; same clamp

# buildCapacityCondition's shared values are SqlParameterToken|undefined. The
# binder only creates non-empty `?N` tokens, so `??` and `||` select the same arm.
src/shared/db/capacity.ts::buildCapacityCondition.listingIdSql~0urfiu2  ?? → ||   # shared.listingId is a non-empty SqlParameterToken or undefined
src/shared/db/capacity.ts::buildCapacityCondition.quantitySql~1gh7q31  ?? → ||   # shared.quantity has the same token domain
src/shared/db/capacity.ts::buildCapacityCondition.excludeAttendeeIdSql~06ef8hm  ?? → ||   # shared.excludeAttendeeId has the same token domain

# Capacity checks clamp a zero-day duration to one day. Demand maps contain
# finite totals, and a missing total has the same zero value as a stored zero.
src/shared/db/attendees/capacity/checks.ts::buildCapacityCheckedInsert.durationDays.durationDays~18guaiu  1 → 0   # dateToStartEnd clamps both duration values to one day
src/shared/db/attendees/capacity/checks.ts::checkListingAvailability.durationDays~18guaiu  1 → 0   # buildCapacityCondition applies the same duration clamp
src/shared/db/attendees/capacity/checks.ts::addDemandToBucket~0k2ebng  ?? → ||   # a supplied duration is a finite integer; zero and the fallback both expand to one clamped day
src/shared/db/attendees/capacity/checks.ts::addDemandToBucket~0k2ebng  1 → 0   # expandDailyRange clamps both fallback values to one day
src/shared/db/attendees/capacity/checks.ts::addDemandToBucket~1bo0bmf  ?? → ||   # a per-day total is finite; zero and a missing value both use zero before the addition

# Atomic line values use non-empty timestamps and numeric package ids. The
# delete pin reads the same stored timestamp domain as the line key.
src/shared/db/attendees/atomic-update.ts::lineBooking.packageGroupId~0djpbjh  ?? → ||   # packageGroupId is undefined or a number; zero and the zero fallback produce the same value
src/shared/db/attendees/atomic-update.ts::lineKeyFromBooking~08fp1gg  ?? → ||   # start_at is null or a non-empty stored timestamp, so both operators produce the timestamp or an empty key part
src/shared/db/attendees/atomic-update.ts::applyAttendeeAtomicEdit.args~16je2gx  ?? → ||   # booking.start_at has the same null-or-non-empty timestamp domain when it becomes the delete pin

# Attendee creation receives validated non-empty discriminators, dates, tokens,
# and finite money values. Empty optional contact fields use empty fallbacks.
src/shared/db/attendees/create.ts::buildAttendeeInsert.kind~0e4oagg  ?? → ||   # kind is undefined or a non-empty AttendeeKind value
src/shared/db/attendees/create.ts::contactInfoFromInput.address~12sc2vz  ?? → ||   # address is a string or undefined, and the empty string equals the fallback
src/shared/db/attendees/create.ts::contactInfoFromInput.phone~1nutvzz  ?? → ||   # phone has the same string-or-undefined domain and empty fallback
src/shared/db/attendees/create.ts::contactInfoFromInput.special_instructions~1butkj0  ?? → ||   # special instructions have the same string-or-undefined domain and empty fallback
src/shared/db/attendees/create.ts::buildAttendeeResult.end_date~1ugulhp  ?? → ||   # durationDays is a finite integer or undefined; zero and the fallback both clamp to one day
src/shared/db/attendees/create.ts::buildAttendeeResult.end_date~1ugulhp  1 → 0   # clampDurationDays changes both defaults to one day
src/shared/db/attendees/create.ts::prepareAttendeeWrite~0lw38vw  ?? → ||   # the negative-quantity guard gives zero and its one fallback the same false result
src/shared/db/attendees/create.ts::prepareAttendeeWrite~0lw38vw  1 → 0   # neither fallback is below zero
src/shared/db/attendees/create.ts::prepareAttendeeWrite.enc~0sjep7t  ?? → ||   # a supplied ticket token is non-empty, and the generated fallback is also non-empty
src/shared/db/attendees/create.ts::prepareAttendeeWrite.activityStatements~15xmc4z  ?? → ||   # source is undefined or a non-empty BookingSource value
src/shared/db/attendees/create.ts::finishAttendeeWrite.attendees.date~1ifkn3h  ?? → ||   # a booking date is nullish or a non-empty validated date
src/shared/db/attendees/create.ts::finishAttendeeWrite.attendees.kind~1udll18  ?? → ||   # kind is undefined or a non-empty AttendeeKind value
src/shared/db/attendees/create.ts::finishAttendeeWrite.attendees.packageGroupId~134nzna  ?? → ||   # packageGroupId is a finite id or undefined; zero equals the fallback
src/shared/db/attendees/create.ts::finishAttendeeWrite.attendees.paymentId~12rd4hg  ?? → ||   # paymentId is a string or undefined, and the empty string equals the fallback
src/shared/db/attendees/create.ts::finishAttendeeWrite.attendees.pricePaid~1f0nm0t  ?? → ||   # pricePaid is finite; zero equals the fallback
src/shared/db/attendees/create.ts::finishAttendeeWrite.attendees.remainingBalance~1wsu743  ?? → ||   # remainingBalance is finite; zero equals the fallback
src/shared/db/attendees/create.ts::finishAttendeeWrite.attendees.statusId~0l3a3of  ?? → ||   # statusId is nullish or a positive stored id, never zero
src/shared/db/attendees/create.ts::provenPiiPaymentSession~12rd4hg  ?? → ||   # paymentId is a string or undefined, and the empty string equals the fallback

# IncompleteBooking never leaves writeWithCreationWork. Its catch uses the
# class identity and returns null, so no caller can read the error name.
src/shared/db/attendees/create-batch.ts::IncompleteBooking~03v6osk  IncompleteBooking → ""   # the internal error is caught by instanceof before its name can be observed

# The image-use collection calls only next and nextMany. These operations read
# the table and scope fields, but they do not read the key field.
src/shared/db/images.ts::getImageFilenamesForItem.stored~0ejzw5s  image → ""   # this query reads one image table, so each selected image column stays unambiguous without its qualifier
src/shared/db/images.ts::getImagesForItem.rows~03xuvta  image → ""   # image_uses has none of the selected image column names, so each unqualified name resolves to images
src/shared/db/images.ts::imageUseOrder.key~0srejmf  image_id → ""   # no production caller uses append, swap, or another ordered-collection operation that reads key

# Group readers return truthy records and arrays. Their batch methods also make
# an entry for each requested key before they read rows.
src/shared/db/groups.ts::getGroupById~1ydaoy4  ?? → ||   # Map.get returns a Group object or undefined, and each Group object is truthy
src/shared/db/groups.ts::getListingsByGroupIds~0zftiii  ?? → ||   # entriesByGroup returns an array or undefined, and every array is truthy
src/shared/db/groups.ts::listingsInGroup~1orw8y7  Missing group listing membership → ""   # getListingsByGroupIds creates an entry for every requested group id, so requiredMapValue cannot raise this message
src/shared/db/groups.ts::packageMembersError.blocked.childIds~05z1ovp  Missing listing child edges → ""   # getIdsByKeys creates an entry for each requested parent id before it reads rows
src/shared/db/groups.ts::packageMembersError.blocked.parentIds~069l6kv  Missing listing parent edges → ""   # getIdsByKeys gives the reverse side the same complete-map guarantee
src/shared/db/groups.ts::packageMembersError~17n4dy8  ?? → ||   # blocked contains non-empty translated error strings, so both operators return the first error or null
src/shared/db/groups.ts::getGroupPackagePrices~1w9nudp  ?? → ||   # Map.groupBy returns a GroupListing array or undefined, and every array is truthy
src/shared/db/groups.ts::loadPackageMemberPricingByGroupIds.rows~19vergh  ?? → ||   # rowsByGroup has the same array-or-undefined domain
src/shared/db/groups.ts::loadPackageMemberPricingByGroupIds.dayPrices~1funuvq  Missing package day prices → ""   # getGroupDayPricesByGroupIds creates an entry for every requested group before it reads rows
src/shared/db/groups.ts::memberQuantityStatement~0ajbifl  ?? → ||   # validated package quantities are positive integers, so zero never reaches this default

# Accounting batch reads return arrays. An empty listing filter skips its SQL
# clause because matchesNothing answers the read before the query runs.
src/shared/accounting/queries.ts::transfersByAccounts~1uo1zuf  ?? → ||   # Map.groupBy returns a transfer wrapper array or undefined, and every array is truthy
src/shared/accounting/queries.ts::listingLegScope.clause~0hguk0m  0 → ""   # matchesNothing is true for this clause, so rowsUnlessNoneMatch returns an empty result without reading clause

# Attendee PII (db/attendees/pii.ts) — three provably-equivalent survivors.
src/shared/db/attendees/pii.ts::buildPiiBlob~1r4bpwk  attendees.pii_blob → ""   # buildPiiBlob receives AttendeePii: every field is already a string, and it supplies the only literal/version fields itself, so the stored-JSON writer cannot reject any contract input; its error label is never observed
src/shared/db/attendees/pii.ts::decryptPiiBlob.lat~0pjzbz9  ?? → ||   # decryptPiiBlob's `blob.la` is string|undefined and the fallback is "": truthy strings pass through, while both undefined and the only falsy string ("") produce "", so `??` and `||` agree on every input
src/shared/db/attendees/pii.ts::decryptPiiBlob.lng~1d5jjcf  ?? → ||   # decryptPiiBlob's `blob.lo` has the same string|undefined domain and "" fallback as latitude, so `??` and `||` agree on every input

# getAttendees filter/query builder (src/shared/db/attendees/select.ts) — the
# defaulted `kind`/`join` operands are string unions that never include "".
src/shared/db/attendees/select.ts::whereClauses.parts.clause~0zb4tlc  ?? → ||   # where.kind: AttendeeKindFilter|undefined — every member ("attendee"/"servicing"/"attendee-or-servicing") is truthy, so `?? "attendee"` and `|| "attendee"` agree on every input
src/shared/db/attendees/select.ts::attendees.join~02qy6z7  ?? → ||   # query.join: AttendeeJoin|undefined — "inner"/"left" are both truthy, so `?? "inner"` and `|| "inner"` agree on every input

# The news card query names a single table, so its columns need no alias to
# resolve.
src/shared/db/news-posts.ts::getNewsPostCards.rows.columns~0dqqett  news_post → ""   # dropping the alias leaves the column names unqualified, and the read selects from one table (news_posts AS news_post) plus scalar subqueries that carry their own qualified id, so every name still resolves to the same column
src/features/admin/api-keys.ts::apiKeyPage.load~0c5nk11  ?? → ||   # entity-page load: find() returns a key row object or undefined — a row object is always truthy, so `?? null` and `|| null` agree on every input

src/shared/db/activity-log.ts::logActivity~078kit2  Activity log row missing → ""   # logActivities returns one row per activity it was given, so the single-activity call always has its row and requireValue never raises this message

# Refund-authority creation uses one SQLite INSERT ... ON CONFLICT DO UPDATE
# ... RETURNING statement. SQLite either returns the inserted/existing row or
# throws; successful execution cannot yield zero rows, so the last-resort error
# label cannot be observed through any database state.
src/shared/sumup/transaction.ts::readSumupTransaction.events~0aphohd  ?? → ||   # validated transaction_events is an array or undefined; every array is truthy, so both operators select the same events or empty fallback

# settleAttendeeRows reads its rows with WHERE payment_session_id IN
# ([...rows.keys()]), so every row the settle callback sees carries a
# sessionId that is a key of that same map. requiredMapValue can never miss
# there, and no input can observe the message on that unreachable throw.
src/shared/db/payment-claim.ts::settleAttendeeRows~1iaqh2o Refund settlement lost a payment row→""   # unreachable: the IN-clause is built from the same map's keys

# backup dump (shared/db/backup-snapshot.ts) — exportTable internals whose
# intermediate values are overwritten or type-guaranteed before any read.
src/shared/db/backup-snapshot.ts::exportTable.colList~1el369a   → "mutated"   # colList is only read when INSERT statements are built, which happens after the first non-empty page assigned it in the rowCount === 0 branch; the initializer is overwritten before every read (an empty table breaks out before any read)
src/shared/db/backup-snapshot.ts::exportTable.rows~0k5qahc  ?? → ||   # suppliedPage is BackupRow[] | undefined: an array (even an empty one) is truthy and undefined is falsy, so ?? and || pick the same side for every possible value
src/shared/db/backup-snapshot.ts::exportTable.colList~0ou5ceg  = → +=   # the rowCount === 0 branch runs at most once (rowCount grows past zero with the same page that enters it), so this appends to the initial empty string, and "" + x === x

# settings/payment-provider.ts — `changePaymentProvider` binds `chosen` for all
# four kinds, but only three of them can read it. The guard above it throws
# unless `kind === "disable"`, so `provider` is a valid PaymentProviderType for
# activate, credentials, and recover. Only `disable` reaches the fallback, and
# `disable` is the one kind whose statement never reads the bound value: its
# `payment_provider` CASE stops at the first WHEN, its
# `last_active_payment_provider` CASE takes the `= 'disable'` branch, and both
# WHERE terms are already true before the `CASE ?` that spells the credential
# key is reached.
src/shared/db/settings/payment-provider.ts::changePaymentProvider.chosen~08zft5u  ?? → ||   # provider is PaymentProviderType | undefined, and no provider id is the empty string, so ?? and || pick the same side
src/shared/db/settings/payment-provider.ts::changePaymentProvider.chosen~08zft5u   → "mutated"   # only kind "disable" takes this fallback, and the disable statement never evaluates a placeholder bound to chosen

# settings/payment-provider.ts — the same shape one line down. The chosen
# provider's credential key fills slot ?4, which the statement reads in exactly
# one place: inside RECOVERY_ALLOWED, behind `?1 <> 'recover'`. Only the
# disable call omits the provider and so takes this empty fallback, and for
# that kind the guard's left side is already true, so the row set is the same
# whatever the key holds.
src/shared/db/settings/payment-provider.ts::changePaymentProvider.chosenCredentialKey~1lnjyfd  → "mutated"   # only kind "disable" takes this fallback, and for that kind RECOVERY_ALLOWED is satisfied before the key is read

# listing-parents.ts — `edgeIncompatibilityAfterChange` reads its two link maps
# with a `?? []` fallback. The maps come from `listingsForLinks`, whose values
# are ListingWithCount[] arrays, so `.get()` is an array or undefined.
src/shared/db/listing-parents.ts::edgeIncompatibilityAfterChange.children~0cip5re  ?? → ||   # childrenByParent.get() is ListingWithCount[] | undefined, and every array is truthy, so ?? and || pick the same side
src/shared/db/listing-parents.ts::edgeIncompatibilityAfterChange.parents~1cm1r1e  ?? → ||   # parentsByChild.get() is ListingWithCount[] | undefined, and every array is truthy, so ?? and || pick the same side

# prune.ts — `paymentStatement` passes the "charge." prefix into the two refund
# authority fragments. Each fragment lands inside a NOT EXISTS / EXISTS subquery
# whose only FROM source is `payment_charges AS charge`, and the fragments name
# only `refund_state_name` and `refund_local_state`, which are payment_charges
# columns. SQLite resolves an unqualified name against the subquery's own FROM
# first, so the empty prefix produces the same statement.
src/shared/db/prune.ts::paymentStatement.sql~1oy3usg  charge. → ""   # the fragment's columns are payment_charges columns and the subquery's only table is charge, so the unqualified names still bind to charge
src/shared/db/prune.ts::paymentStatement.sql~0tukn2g  charge. → ""   # same subquery shape one arm down: the unqualified names still bind to charge

# payment-finalize.ts — the stored reference fields fall back to "". Each left
# side is a string or undefined, and the fallback is the empty string, so ??
# and || agree on every value: a truthy string passes through both, undefined
# takes the fallback in both, and an empty string yields "" either way.
src/shared/db/payment-finalize.ts::buildFinalizeStatements.args~10demh7  ?? → ||   # stored?.encrypted is string | undefined with an empty-string fallback, so both operators agree on every value
src/shared/db/payment-finalize.ts::buildFinalizeStatements.args~0clq12z  ?? → ||   # stored?.index is string | undefined with an empty-string fallback, so both operators agree on every value
