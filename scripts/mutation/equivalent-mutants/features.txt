# Known-equivalent mutants — see README.txt in this directory.

src/features/admin/entity-pages.ts::defineEntityPage.renderPage.ctx.baseUrl~0v3px31  ?? → ||   # opts.baseUrl is a string and the only falsy string is the empty fallback itself
src/features/admin/entity-pages.ts::defineEntityPage.renderPage.ctx.query~1xs3bh3  ?? → ||   # opts.query is a URLSearchParams object when present, so it is always truthy
src/features/admin/site-content-page.ts::defineSiteContentPage.tabs~0786g3u  ?? → ||   # extraTabs is an array when present, and arrays are always truthy
src/ui/templates/admin/holidays.tsx::HolidayEditPanel.html~1nrsidb  ?? → ||   # values is a render-values object when present, so it is always truthy
src/ui/client/admin/manual-checkin.ts::initManualCheckin.filterOptions.text~0hoqwoy  ?? → ||   # textContent is string|null; its only falsy string is the empty fallback itself
src/ui/client/admin/order-gallery.ts::initOrderGallery.refresh.timer~1xo126t  ?? → ||   # states is a record object when present, so it is always truthy

# ticket-payment values whose present form is always truthy, whose falsy value
# equals the fallback, or whose mutated constant is normalized before use.
src/features/public/ticket-payment.ts::checkAvailability~0dpsw9a  ?? → ||   # date is nullish or a non-empty ISO date; only the nullish values use the null fallback
src/features/public/ticket-payment.ts::checkAvailability.dayCount~0fy6gm8  1 → 0   # bookingDateFields clamps either default day count to one, so the produced availability rows are identical
src/features/public/ticket-payment.ts::resolveDayCount.shownName~037zc5x  ?? → ||   # a package stand-in is a non-empty group name or undefined, so only undefined reaches the listing-name fallback
src/features/public/ticket-payment.ts::foldSelectedChildren.tree~12uyd8g  ?? → ||   # prebuiltTree is a BookingTree object when present, and objects are always truthy
src/features/public/ticket-payment.ts::foldSelectedChildren.hasFoldableParent~08cbwkz  ?? → ||   # base.quantities.get(): number|undefined; the only falsy number is 0, 0??0===0||0
src/features/public/ticket-payment.ts::EMPTY_PRICED_ORDER.fullSubtotal~16fny0e  0 → 1   # EMPTY_PRICED_ORDER.fullSubtotal is never read: bookingFactsFromOrder derives its facts from total, extras, lines, and modifierApplications
src/features/public/ticket-payment.ts::createFreeReservation.dayCount.dayCount~0fy6gm8  1 → 0   # bookingsForOrder normalizes either default day count to one, so the stored booking duration is identical
src/features/public/ticket-payment.ts::createFreeReservation.result.pricedOrder~1mtrtps  ?? → ||   # ledgerOrder: PricedOrder|null — an object is always truthy, null→fallback either way
src/features/public/ticket-payment.ts::singleDailyParent.children~1kv3u53  ?? → ||   # childrenByParentId.get(): TicketListing[]|undefined — an array is always truthy, undefined→null either way
src/features/public/ticket-payment.ts::getTicketContext.packages~0m8qaqi  ?? → ||   # pagePackages is an array when present, and arrays are always truthy
src/features/public/ticket-payment.ts::loadPagePackages~0vaeh8c  Missing package pricing → ""   # loadPackageMemberPricingByGroupIds seeds an entry for every group id it is given, and only the page's own package ids are looked up, so requiredMapValue never raises this message

# attendee-form-model: `?? → ||` where the left operand can never be a
# falsy-but-non-null value, so the two operators always agree.
src/features/admin/attendee-form-model.ts::parseAttendeeForm.lines.existingBooking~1genp17  ?? → ||   # existingByKey.get(key): ListingAttendeeRow|undefined — an object is always truthy
src/features/admin/attendee-form-model.ts::parseAttendeeForm.lines.listing~1y7j49h  ?? → ||   # listingsById.get(id): ListingWithCount|undefined — an object is always truthy
src/features/admin/attendee-form-model.ts::isPaymentLockedLine~1cyk75e  ?? → ||   # (existingBooking?.price_paid ?? 0): a number; the only falsy number is 0, and 0 ?? 0 === 0 || 0
src/features/admin/attendee-page-data.ts::loadPackagePaths.memberPrices~1xcdymp  ?? → ||   # priceRowsByGroupId.get(): PriceRow[]|undefined — an array is always truthy
src/features/admin/attendee-page-data.ts::packagesByListingIdFrom.groups~183l51m  ?? → ||   # byListing.get(): Map<number,number|null>|undefined — a Map is always truthy
src/features/admin/attendee-page-data.ts::buildFormLines.standaloneBlanks~11c4q19  ?? → ||   # preselectedQty.get(): number|undefined; the only falsy number is 0, and the fallback is also 0
src/features/admin/attendee-page-data.ts::listingBookingFor.durationDays~1vg0u7m  1 → 0   # standard bookings pass date:null, so buildCapacityCondition ignores durationDays entirely
src/features/admin/attendee-page-data.ts::overbookedListingIds~11ijbqo  ?? → ||   # totalByListing.get(): number|undefined; the only falsy number is 0, and the fallback is also 0
src/features/admin/attendee-page-data.ts::computeWarnings.warns~1e9bm1v  ?? → ||   # incompleteParents stores only a non-empty translated warning string, otherwise get returns undefined
src/features/admin/attendee-page-data.ts::assembleTemplateData.balanceNotice~0gnct02  ?? → ||   # find returns a status object or undefined, and a status object is always truthy
src/features/admin/attendee-page-data.ts::assembleTemplateData.balanceNotice~0axstbd  ?? → ||   # remaining_balance is an integer; its only falsy value is 0, which equals the fallback
src/features/admin/attendee-page-data.ts::assembleTemplateData.balanceNotice~0g9qz7c  ?? → ||   # fullPrice is an integer or the summary is absent; 0 equals the fallback and every other amount is truthy
src/features/admin/attendee-page-data.ts::assembleTemplateData.balanceNotice~0ec14e6  ?? → ||   # depositPaid is an integer or the summary is absent; 0 equals the fallback and every other amount is truthy
src/features/admin/attendee-page-data.ts::assembleTemplateData.hasMixedTimings~1hmlq9c  ?? → ||   # hasMixedTimings is boolean|undefined, and false equals the fallback
src/features/admin/attendee-page-data.ts::assembleTemplateData.questions~0wxrma2  ?? → ||   # questions is an array when present, and arrays are always truthy
src/features/admin/attendee-page-data.ts::assembleTemplateData.selectedAnswerIds~1og9d80  ?? → ||   # selectedAnswerIds is an array when present, and arrays are always truthy
src/features/admin/attendee-page-data.ts::assembleTemplateData.selectedTextAnswers~0av0z2p  ?? → ||   # selectedTextAnswers is a Map when present, and Maps are always truthy
src/features/admin/attendee-page-data.ts::loadQuestionsForExisting.selectedAnswerIds~0qpejdr  ?? → ||   # attendeeAnswerMap values are arrays, and arrays are always truthy
src/features/tickets/token-utils.ts::verifyTokensWithRealLine.verified.realBookings~1jqkiac  ?? → ||   # (attendees[i]?.bookings ?? []): ListingAttendeeRow[]|undefined — an array is always truthy, undefined→[] either way

# Ticket form parsing (ticket-form.ts): `?? → ||` where the operand is an array,
# an object, a Map.get number (only falsy-non-null is 0, 0??0===0||0), or a
# string that is never the empty string — so the two operators always agree.
src/features/public/ticket-form.ts::collectValuesByListing.listingIds~0go5ifo  ?? → ||   # questionListingMap.get(): number[]|undefined — arrays always truthy
src/features/public/ticket-form.ts::ticketFormErrorResponse.url~0i79mbu  ?? → ||   # ctx.actionUrl: string|undefined, only ever a real URL or undefined (never ""), so ?? and || agree
src/features/public/ticket-form.ts::listingsWithQuantity.withQty.qty~0q3jgeq  ?? → ||   # quantities.get(): number|undefined; the only falsy number is 0, 0??0===0||0

# Admin listing API fallbacks receive only objects, arrays, positive ids, or
# nullish values, so no valid request can distinguish these operators.
src/features/admin/api.ts::bodyToUpdateInput.existing~0xlflfs  ?? → ||   # stored listing lookup returns a ListingWithCount object or null; listing objects are truthy
src/features/admin/api.ts::bodyToUpdateInput.groupIds~0nj5jla  ?? → ||   # parsed group ids are an array or undefined; every array, including [], is truthy
src/features/admin/api.ts::UNCREATED_PARENT_ID~05rrw2z  1 → 0   # the mutation changes sentinel -1 to -0; positive listing ids match neither value
src/features/admin/api.ts::prepareListingJoins.packageConflict~1juqee2  ?? → ||   # groupIds is an array or undefined; every array, including [], is truthy
src/features/admin/api.ts::prepareListingJoins.result~0i1al4o  ?? → ||   # an existing listing id is positive and truthy, otherwise optional access yields undefined
src/features/admin/api.ts::prepareListingJoins.result.wouldBeGroupIds~1juqee2  ?? → ||   # groupIds is an array or undefined; every array, including [], is truthy

# attendee entity page: `?? → ||` where the operand's only falsy-non-null
# value equals the "" fallback.
src/features/admin/attendee-page.ts::attendeePage.tabs.sections.load~1e06wxq  ?? → ||   # ctx.query.get("token"): string|null; the only falsy-non-null value "" equals the "" fallback
src/shared/db/name-registry.ts::matchName.ids~140086l  ?? → ||   # matchName index.get(): number[]|undefined — an array is always truthy
src/shared/db/name-registry.ts::isNameTakenAnywhere.ownedByOther~1x88e07  ?? → ||   # isNameTakenAnywhere index.get(): number[]|undefined — an array is always truthy
src/features/admin/catalog-transfer/import.ts::membershipSpec.quantity~0yrngiz  ?? → ||   # membershipSpec quantity comes from the PositiveInt schema (at least 1) or is undefined, so it is never 0
src/features/admin/catalog-transfer/import.ts::loadChildAddOnChecker.wouldBe.active~1hbkt0d  true → false   # childOnlyAddOnCheckerForListings resolves only ids and groupIds; it never reads ListingGroupMembership.active
src/features/admin/catalog-transfer/import.ts::loadParentEdgeContext.childEdge~08stb2y  0 → 1   # edgeFieldError never reads EdgeListing.id, and child reachability uses the separate placeholder id declared above
src/features/admin/catalog-transfer/import.ts::importGroup.isPackage~0u7qcwn  ?? → ||   # isPackage is boolean|undefined, and false equals the false fallback
src/features/admin/catalog-transfer/schema.ts::formatTransferIssues.parts~0f9t11y  ?? → ||   # formatTransferIssues flat.root: string[]|undefined — an array is always truthy
src/features/admin/catalog-transfer/schema.ts::formatTransferIssues.parts~19zbaq1  ?? → ||   # formatTransferIssues flat.nested: object|undefined — an object is always truthy

# Admin group routes (features/admin/groups.ts) — object/Map/0-fallback nullish
# coalescing where ?? and || always agree.
src/features/admin/groups.ts::parseMemberDayPrices.dayPrices~0mwoiz0  ?? → ||   # parseMemberDayPrices byListing.get(): DayPrices|undefined — an object is always truthy
src/features/admin/groups.ts::parsePackageMembers.dayPrices~1s9d862  ?? → ||   # parsePackageMembers dayPricesByListing.get(): DayPrices|undefined — an object is always truthy
src/shared/listings-actions.ts::strippedPageOrphanedAddOn~05ffdhd  && → ||   # clearing false means mayStripPage never loads a flagged child; clearing true without a flagged child only reruns unchanged reachability, which cannot create an orphan

# attendee merge/refund route: mutants with no observable effect.
src/features/admin/attendee-refunds.ts::handleAdminRefundAllGet~0mgteeu  ?? → ||   # flash.error: string|undefined from applyFlash; a refund error flash is always a non-empty message, so the only falsy-non-null value "" never occurs and ?? / || agree

# middleware.ts's Content-Type fallback already equals the only falsy string.
# Once a tracking key is found, continuing instead of breaking can only
# rediscover more tracking keys without changing the final boolean.
src/features/middleware.ts::lowerContentType~06bb1lp  ?? → ||   # Content-Type is string|null; the only falsy non-null string is "", which is also the fallback
src/features/middleware.ts::getCleanUrl~0l5b2wp  break; → (removed)   # later keys cannot reverse hasTracking

# group-page-data fallbacks have operands whose only falsy non-null value
# coincides with the fallback.
src/features/admin/group-page-data.ts::groupHasPaidListing~154y783  ?? → ||   # (row.package_price ?? 0): number|null, only falsy-non-null is 0 and 0 ?? 0 === 0 || 0
src/features/admin/group-page-data.ts::loadGroupEditPanel.members.dayPrices~1i7vpix  ?? → ||   # dayPrices.get(): DayPrices (a Map) | undefined — a Map is always truthy

# Admin attendee-merge routes (src/features/admin/attendees-merge.ts) — mutants
# with no observable effect through any reachable caller or input.
src/features/admin/attendees-merge.ts::updateTargetPiiFromDecision.keptIsPinned~18lqcgb  && → ||   # keptIsPinned = Boolean(kept.lat && kept.lng): lat/lng are always both non-empty or both "" (parseCoordinatePair requires both-or-neither, and every write path — the logistics route, the attendee-form addressUnchanged branch, and the merge's own addressFrom — preserves that pair), so `&&` and `||` agree on every reachable value
src/features/admin/attendees-merge.ts::toBookingChoice~033wh8z  skip_source → ""   # toBookingChoice comparison `raw === "skip_source"` → `raw === ""`: parse "skip_source" as the "keep_target" fallback. Unobservable — both fall to applyBookingDecisions' same `else` branch (no DB write, bookingsSkipped++) and discardedSaleAmount returns sourceSaleAmount for both, so the merge outcome is identical for every input
# contact-tokens.ts — `?? → ||` sites whose operand can never be a
# falsy-but-non-null value.
src/shared/db/contact-tokens.ts::loadTokenBlob~0hgbweg  ?? → ||   # loadTokenBlob's `row?.attendee_tokens_blob ?? null`: attendee_tokens_blob is string|undefined; the only falsy-non-null is "" and tokenLinesFrom("") === tokenLinesFrom(null) === [], so ?? and || agree
src/shared/db/contact-tokens.ts::syncChannelToken~07hz79m  ?? → ||   # `removedSource ?? sync.source`: removedSource is BookingSource|null, and both BookingSource values ("admin","public") are non-empty truthy strings, so it is never falsy-but-non-null; ?? and || agree

# Admin segment dispatch (admin/index.ts) — two provably-equivalent survivors
# from the routeAdmin/adminPathSegment run over the manifest + footer suites.
src/features/admin/index.ts::adminPathSegment~1hrcxzo  ?? → ||   # `path.split("/")[2] ?? ""`: the RHS is "" and the only falsy string is "", so `x ?? ""` and `x || ""` agree on every string|undefined input
src/features/admin/index.ts::buildSegmentRouters.areas~1hp6gg8  ?? → ||   # areasBySegment values are arrays, so every present value is truthy and a miss is undefined

# App prefix dispatch (app/routes.ts) — two provably-equivalent survivors.
src/features/app/routes.ts::publicPagePath~05stvfg   → "mutated"   # publicPagePath only receives the declared prefixes "", "listings", and "terms"; for "", both ternary arms return "/", and neither non-empty prefix equals "mutated", so no declared route path changes
src/features/app/routes.ts::routeMainApp~04n7sdh  ?? → ||   # route handlers return Response|null; every Response object is truthy, while null (and the mutation runner's return-undefined mutants) selects notFoundResponse under both operators

# Admin refund waves (refunds/waves.ts) — one provably-equivalent survivor from
# the packByReferenceCount run.
src/features/admin/refunds/waves.ts::packByReferenceCount.currentCount~0bth322  0 → 1   # packByReferenceCount's `currentCount` init: the first iteration always hits `!wave` (waves is empty, so waves[-1] is undefined), which short-circuits the `|| currentCount + refs > budget` before currentCount is read; every later iteration reassigns it first, so the initial value is never observed

# listing edit panel (features/admin/listing-page-management-panels.ts) — group-id fallback.
src/features/admin/listing-page-management-panels.ts::loadListingEditPanel.selectedGroupIds~0pbud0y  ?? → ||   # `selectedGroupIds ?? ctxData.selectedGroupIds`: selectedGroupIds is number[]|undefined — a submitted array (even []) is always truthy, so both operators return it unchanged, and undefined maps to the stored ids under both. No input distinguishes them (same class as attributes.tsx:173:61)

# Strict-null cleanup source files — values where `??` and `||` agree, plus
# assignments and SQL aliases whose mutations cannot change an observable result.
src/features/admin/calendar-csv.ts::logisticsColumns.agentName~0meqtch  ?? → ||   # agentNames.get() is string|undefined; "" and the "" fallback are identical
src/features/admin/calendar-csv.ts::logisticsColumns.value~00m7ij3  ?? → ||   # startTime is string|undefined; every falsy value produces the same "" fallback
src/features/admin/calendar-csv.ts::logisticsColumns.value~1mtw1ii  ?? → ||   # endTime is string|undefined; every falsy value produces the same "" fallback
src/shared/forms/rendering.tsx::splitDatetime.date~1f0kxzu   → "mutated"   # split("T")[0] always exists, and the empty input returned before this line
src/shared/forms/rendering.tsx::publicLinkHint~0wwelzf  || → &&   # publicLinkPath is absent or a function, so both boolean terms always agree
src/shared/forms/rendering.tsx::renderField~1vcsvsj  ?? → ||   # beforeHtml is string|undefined and falls back to "", so both operators agree
src/shared/runtime.ts::buildRuntimeInfo.arch~00dnfok  ?? → ||   # arch is string|undefined and falls back to "", so both operators agree
src/shared/runtime.ts::buildRuntimeInfo.denoVersion~1pyb0qg  ?? → ||   # denoVersion is string|undefined and falls back to "", so both operators agree
src/shared/runtime.ts::buildRuntimeInfo.nodeCompatVersion~1m2e610  ?? → ||   # nodeCompatVersion is string|undefined and falls back to "", so both operators agree
src/shared/runtime.ts::buildRuntimeInfo.os~05o6squ  ?? → ||   # os is string|undefined and falls back to "", so both operators agree
src/shared/runtime.ts::buildRuntimeInfo.typescriptVersion~14e4wx7  ?? → ||   # typescriptVersion is string|undefined and falls back to "", so both operators agree
src/shared/runtime.ts::buildRuntimeInfo.userAgent~17udaa2  ?? → ||   # userAgent is string|undefined and falls back to "", so both operators agree
src/shared/square.ts::createSquareClient.checkout.paymentLinks.create.data.pre_populated_data~0evahy5  ?: → consequent only   # the buyer_phone_number ternary spreads {} when the phone is absent; the consequent-only mutant always spreads { buyer_phone_number: undefined }, but JSON.stringify omits undefined values, so the serialized request body is identical in both cases
src/shared/square.ts::squareApi.testSquareConnection.locations~10z1q7q  ?? → ||   # locations is an array or undefined; arrays are truthy and undefined takes [] under both operators
src/ui/client/dom.ts::createButton.button%2eclassName~16pvzvs  = → +=   # createElement returns a new button with className "", so both assignments produce the supplied class string

# Paid-payment processing: private defaults are always supplied by their only
# callers; optional collections are arrays (always truthy); parsed day counts and
# stored package quantities are positive; equal zero fallbacks stay equal.
src/features/api/payment-processing/index.ts::replaySuccess.paymentReference~12k7fdb   → "mutated"   # replaySuccess is private and both callers always pass session.paymentReference, so the default is never evaluated
src/features/api/payment-processing/items.ts::bookingPaths.allocations~1sr8ybw  ?? → ||   # intent.allocations is an array or undefined, and arrays (including []) are truthy
src/features/api/payment-processing/items.ts::validateAllItems.expectedPrice~0mb68sn  ?? → ||   # extractIntent emits a positive dayCount or undefined, so every present value is truthy
src/features/api/payment-processing/pricing.ts::paidByItem.current~0h5qe1t  ?? → ||   # paidByItem stores numeric charged totals; the only falsy present total is 0, and both operators keep the 0 fallback
src/features/api/payment-processing/package-pricing.ts::allocatedUnitsByChild~1sr8ybw  ?? → ||   # intent.allocations is an array or undefined, and arrays (including []) are truthy
src/features/api/payment-processing/package-pricing.ts::allocatedUnitsByChild.prior~0fwromb  ?? → ||   # the running allocated quantity is non-negative and the fallback is 0, so 0 ?? 0 and 0 || 0 agree
src/features/api/payment-processing/package-pricing.ts::packageBundleMismatch.count~0paa7g7  ?? → ||   # stored per-package quantities are at least 1, so every present value is truthy
src/features/api/payment-processing/package-pricing.ts::orderEdgeDrifted.fullyFolded~0b7s0d3  ?? → ||   # the allocated quantity is non-negative and the fallback is 0, so both operators agree for 0 and undefined
src/features/api/payment-processing/package-pricing.ts::orderEdgeDrifted.topLevel~1g6e18j  false → true   # orderEdgeDrifted uses buildTicketListing only to pass its listing row into buildBookingTree; the closed/availability fields never affect node keys
src/features/api/payment-processing/package-pricing.ts::orderEdgeDrifted.childrenByParentId~0iift5t  false → true   # child TicketListing availability is likewise unread by buildBookingTree's node-key construction
src/features/api/payment-processing/package-pricing.ts::orderEdgeDrifted.packages.hideListings~0xzhsy5  false → true   # hideListings changes node visibility only, while edgeDrifted reads node keys and child structure, never visibility
src/features/api/payment-processing/package-pricing.ts::orderEdgeDrifted~1sr8ybw  ?? → ||   # intent.allocations is an array or undefined, and arrays (including []) are truthy
src/features/api/payment-processing/package-pricing.ts::hasStaleStandaloneChild.allocated~0ystvom  ?? → ||   # the allocated quantity is non-negative and the fallback is 0, so both operators agree for 0 and undefined

# Payment success replay only loads a listing fallback while thankYouUrl is the
# empty string, so assignment and append produce the same string for every path.
src/features/api/webhooks.ts::processSessionAndRedirect.thankYouUrl~1sbbxjk  = → +=   # the enclosing !thankYouUrl guard narrows the string to "" before this assignment, and "" + fallback equals fallback
src/ui/templates/admin/settings/payment.tsx::shownPaymentProvider~1empuiw  ?? → ||   # both operands are PaymentProviderType|null; every provider id is non-empty and truthy, while null selects the same fallback under either operator

# Folded API booking: equivalents for schema-validated inputs and unused paths.
src/features/api/folded-booking.ts::applyChildSelectionsToForm.childBySlug~0ucmfin  ?? → ||   # childrenByParentId is Map<number, TicketListing[]>; get returns an array (always truthy, even []) or undefined, so ?? and || agree
src/features/api/folded-booking.ts::applyChildSelectionsToForm~1mf4dzi  ?? → ||   # qtyByChild accumulates ApiQuantitySchema values (positive integers >= 1) or is unset; it is never 0 or NaN for validated inputs, so ?? 0 and || 0 agree
src/features/api/folded-booking.ts::foldedIntent~0frnutw  1 → 0   # the thankYouUrl guard checks fold.listings.length > 1 only when parentThankYouUrl is truthy (parent flow), which always folds parent + >= 1 child (length >= 2); package callers pass no parentThankYouUrl, so the && short-circuits and the check never runs
src/features/api/folded-booking.ts::completeFoldedBooking.reservation.ledgerOrder.feeSubtotal~1szm9if  0 → 1   # feeSubtotal flows into priceCheckout → owedOrderForLedger, which drops all extras and sets total to 0; bookingFactsFromOrder reads extras (empty) → bookingFee: 0 regardless. The feeSubtotal value never reaches any observable output.
src/features/api/folded-booking.ts::processParentApiBooking.built.dayCount~1moogp1  1 → 0   # base.dayCount is read only by parentResolvedDuration → childDaysFromParent(parent, dayCount, 1), which uses its first argument (dayCount) only in the parent.customisable_days branch; processParentApiBooking rejects customisable parents at line 335 before reaching the fold, so that branch is unreachable. For daily parents the function returns clampDurationDays(parent.duration_days), and for standard parents it returns 1 (the standardValue) — neither reads dayCount, so 1 and 0 produce the same duration and thus the same fold output for every reachable input.

# question answers (features/admin/questions/answers.ts) — the ordered-insert
# placeholder and a throw-only label that can never throw.
src/features/admin/questions/answers.ts::handleAddAnswer.onValid.sortOrder~1g336tf  0 → 1   # the inserted sortOrder is a placeholder: insertScopedOrderedRow runs order.append in the same transaction, and append sets sort_order from MAX over siblings that explicitly exclude the row itself, then overwrites it unconditionally before commit — the placeholder value is never read by anything
src/features/admin/questions/forms.ts::questionTextForm.fields.options~0y51s1u  Display as → ""   # requireChoiceOptions reads its label only in its two throw paths: an empty options list, or a value failing an invalid-rule. QUESTION_DISPLAY_TYPES is a non-empty picklist constant and requireChoiceOptions carries no invalid rule, so neither throw is reachable and the label is never read

# Cart package resolution (features/public/groups.ts): map lookups whose key is
# guaranteed present, and a nullable-object fallback.
src/features/public/groups.ts::loadCartPackagesBySlugs.completeById~0c1aa2y  Missing package members → ""   # getListingsByGroupIds seeds an entry for every group id it is given, and the lookup uses ids taken from that same list, so requiredMapValue never raises this message
src/features/public/groups.ts::loadCartPackagesBySlugs.completeById~0ywy95s  Missing package membership → ""   # groupListings.getIdsByKeys likewise returns an entry for every requested key, so this message is unreachable
src/features/public/groups.ts::loadCartPackagesBySlugs~1y9lq6a  ?? → ||   # completeById holds GroupWithListings objects or null, so a present value is always truthy and ?? null and || null agree for object, null and undefined alike

# Cart slug resolution (features/public/cart.ts): nullable-object fallbacks and
# a repeat guard whose effect a downstream dedupe already guarantees.
src/features/public/cart.ts::cartPackagesBySlug.packageSlugs~10kkd9l  ?? → ||   # getListingsBySlugs yields a ListingWithCount object or null per slug, so a present value is always truthy and both operators give the same null
src/features/public/cart.ts::resolveCartSlugs.listing~10kkd9l  ?? → ||   # same per-slug listing lookup, same object-or-null values
src/features/public/cart.ts::resolveCartSlugs~053nnt8  standaloneIds.add(listing.id); → (removed)   # the set only skips an exact repeat of one listing slug, and cartListings runs uniqueBy on listing id afterward; the other readers of items are a Set of solo child ids and the package-only loop, so a repeated standalone item cannot change any output
src/features/public/cart.ts::resolveCartSlugs~1nfyz8t@2  continue; → (removed)   # without it a slug that already resolved to a listing falls into the package lookup, but only slugs with NO listing are put in packagesBySlug, so the lookup always misses and the loop continues anyway

# Listing detail group context (features/admin/listings-view.ts).
src/features/admin/listings-view.ts::loadGroupContext.remaining~08seojj  Missing group remaining → ""   # getGroupRemainingByGroupId returns an entry for every group id whose cap is positive, and only those ids are looked up, so requiredMapValue never raises this message
src/features/api/payment-processing/package-pricing.ts::loadPackagePricingByGroup.pricing~1bkld7z  Missing package pricing → ""   # loadPackageMemberPricingByGroupIds is given exactly the order's group ids, and only those are looked up, so requiredMapValue never raises this message

# Page packages (shared/booking/page-packages.ts): nullish fallbacks on values
# that are only ever a number-or-undefined with a matching fallback.
src/shared/booking/page-packages.ts::soleParentPackageIds.groupId~0djpbjh  ?? → ||   # packageGroupId is a positive group id or undefined; its only falsy value would be 0, which equals the fallback
src/shared/booking/page-packages.ts::stampChildRowPackages~1pnj0bd  ?? → ||   # same package group id, same 0 fallback
src/shared/booking/page-packages.ts::stampChildRowPackages.parentId~0mh8ima  ?? → ||   # parentListingId is a positive listing id or undefined, and the fallback is 0

# Payment provider setup and the balance notice (main #2020): a paid amount that
# cannot reach any branch, and a recovery count that is never exactly one.
src/features/admin/attendee-page-data.ts::assembleTemplateData.balanceNotice~0ec14e6  0 → 1   # the fallback is used only in create mode, where fullPrice is 0; the amount paid cannot affect any balance-notice branch when no order exists
src/features/admin/settings-page.ts::getAdvancedSettingsPageState.paymentProviderRecoveryNeeded~1fd1z1o  0 → 1   # existingPaymentProviderState resolves one configured provider directly; recoveryChoices is therefore either empty or has at least two providers, so both comparisons agree

# Public order page (order.ts) — booking-span sums whose floor the capacity
# reading applies again, and key halves that are always present.
src/features/public/order.ts::bookingSpanDays~1ybe7gz  1 → 0   # a stored daily listing's duration_days is at least 1, so Math.max never picks the floor
src/features/public/order.ts::bookingSpanDays~1mmd8yc  1 → 0   # the span of a listing not judged day by day is unused, and daysOfSpan clamps it back to one day anyway
src/features/public/order.ts::widestSpanPerGroup~0ia21ee  ?? → ||   # spanByGroupId holds spans of at least 1, so the only falsy value is undefined
src/features/public/order.ts::widestSpanPerGroup~0ia21ee  1 → 0   # the first span seen is at least 1, so Math.max returns it whichever starting value it is compared against
src/features/public/order.ts::loadOrderPools.spanOf~13yakgc  1 → 0   # with no date chosen nothing is judged day by day, and daysOfSpan clamps the span back to one day
src/features/public/order.ts::loadOrderPools~15mruqs  1 → 0   # every listing's span is at least 1, so the floor never wins; with no listings at all the reading has no days either way
src/features/public/order.ts::bookingUrlFor.chosen.kind~17n3p4g   → "mutated"   # a selection key is always built as kind:id by listingOptionKey/packageOptionKey, so the kind is never missing
src/features/public/order.ts::bookingUrlFor.chosen.rawId~1xqr8m1   → "mutated"   # the same key always carries its id, so the id half is never missing
src/features/public/order.ts::bookingUrlFor.chosen.prefill~1vgmybj  1 → 0   # buildTicketListing sets maxPurchasable to 0 for a sold-out or closed listing and to at least 1 otherwise (max_quantity is at least 1), so the two thresholds never disagree once the checks beside them have passed

# Listing parent editor: present link arrays and cloned ids are truthy, and the
# in-memory child add-on resolver reads group ids but not listing activity.
src/features/admin/listings-parents.ts::loadListingParentsSection.offeredUnder~0sst7oc  ?? → ||   # listingsByKey.get() returns an array or undefined; arrays are always truthy
src/features/admin/listings-parents.ts::childOnlyAddOnResolver.allListings.active~1hbkt0d  true → false   # childOnlyAddOnNameForListings expands group ids only and never reads ListingGroupMembership.active
src/features/admin/listings-parents.ts::remapDuplicatedGroupEdges.toEdge.childId~1us6y6n  ?? → ||   # clone ids and listing ids are positive, so both operands are always truthy when present
