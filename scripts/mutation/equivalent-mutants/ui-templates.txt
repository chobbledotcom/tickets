# Known-equivalent mutants — see README.txt in this directory.

# Parent/child curried atoms (shared.tsx).
src/ui/templates/layout.tsx::Layout.resolvedTheme~0oltraa  ?? → ||   # Theme is "light"|"dark" or undefined, so every present value is truthy

# Parent/child per-unit render (reservations contact-fields.ts).
src/ui/templates/public/reservations/contact-fields.ts::pagePaid~07x8o1n  ?? → ||   # addOns?.some(...) is boolean|undefined; ?? and || agree on every boolean and on undefined
src/ui/templates/public/reservations/form.tsx::TicketPageForm.html~1470qil   → "mutated"   # TicketPageForm's only caller supplies YYYY-MM-DD options; the fallback affects HTML only if it equals an option, which "mutated" never can
src/ui/templates/public/reservations/ticket-page.tsx::ticketPage~0nmjfxs  ?? → ||   # questions is QuestionWithAnswers[]|undefined, and every present array, including [], is truthy
src/ui/templates/public/reservations/ticket-page.tsx::ticketPage~0pcr5er  ?? → ||   # childDatesById is a ReadonlyMap|undefined, and every present Map, including an empty one, is truthy
src/ui/templates/public/reservations/ticket-page.tsx::cartConflicts.concealed.childrenByParentId~1gqpisn  ?? → ||   # childrenByParentId is Map<...>|undefined, and every present Map, including an empty one, is truthy — no falsy-non-null value exists, so ?? and || pick the same operand

# attendee table values and columns
src/ui/templates/attendee-table/values.ts::getAnswerDisplay.answerIds~1spki67  ?? → ||   # getAnswerDisplay attendeeAnswerMap.get(): number[]|undefined — an array is always truthy, so ?? and || coincide
src/ui/templates/attendee-table/columns.tsx::date.rawValue~0qlr1fl  || → ??   # date column row.attendee.date: string|null; null gives "" under both operators, and "" is the only falsy-non-null string, which also gives "" under both
src/ui/templates/attendee-table/columns.tsx::email.cell~1c5tvxb  || → ??   # email column row.attendee.email: string (never null/undefined); "" is the only falsy value and is also the fallback, so || and ?? agree for every possible value
src/ui/templates/attendee-table/values.ts::ATTENDEE_ROW_ORDER~0rc55ig  ?? → ||   # listing name is string|undefined; its only falsy string is the empty fallback, so ?? and || agree

# attributes detail page (ui/templates/admin/attributes.tsx) — option listing count fallback.
src/ui/templates/admin/attributes.tsx::adminAttributePage.count~1lrxphe  ?? → ||   # `data.listingCounts.get(option.id) ?? 0`: listingCounts is Map<number,number> built from ids.length, so a present count is always >= 1 and an absent option returns undefined — the left side is never a falsy non-null number, so `?? 0` and `|| 0` agree on every reachable input (same class as checkout-pricing.ts:85:35 and pr-queue/render.ts:93:29)

# questions templates (ui/templates/admin/questions.tsx) — Map.get fallbacks whose
# left operand can never be a falsy-but-non-null value, so `??` and `||` always agree.
src/ui/templates/admin/questions.tsx::adminQuestionPage.count~13bitn3  ?? → ||   # `answerCounts?.get(a.id) ?? 0`: answerCounts is Map<number,number>|undefined, so the left side is a number or undefined. The only falsy-non-null number is 0, and 0 ?? 0 === 0 || 0; undefined maps to 0 under both — no input distinguishes them (same class as attributes.tsx:173:61 and checkout-pricing.ts:85:35)
# cache-registry table sets: invalidatorsByTable.get() is Set|undefined, and a
# Set object is always truthy, so `?? new Set()` and `|| new Set()` agree.

# questions list page (ui/templates/admin/questions.tsx) — column keys the page
# never reads back, and a Map.get fallback over always-truthy arrays.
src/ui/templates/admin/questions.tsx::questionListingsFor.listingNames~1vj5u51  ?? → ||   # `listingNames.get(question.id) ?? []`: listingNames is Map<number, string[]>, so the left side is an array (always truthy) or undefined — no falsy-non-null value exists, and ?? and || agree on every input (same class as questions.tsx:270:50)
src/ui/templates/admin/questions.tsx::adminQuestionsPage.columns.key~0eka7sm  question → ""   # a column `key` is read only by renderTable's columnKeys/hiddenKeys lookups and saved-filter value rendering; reorderableListPage renders the questions table with only reorder options — no columnKeys, hiddenKeys, or filter — so this key is never read
src/ui/templates/admin/questions.tsx::adminQuestionsPage.columns.key~1c98v7j  answers → ""   # same as questions.tsx:170:14 — the questions list table is rendered without columnKeys, hiddenKeys, or filters, so its column keys are never read
src/ui/templates/admin/questions.tsx::adminQuestionsPage.columns.key~08wyblh  listings → ""   # same as questions.tsx:170:14 — the questions list table is rendered without columnKeys, hiddenKeys, or filters, so its column keys are never read

# backups page (ui/templates/admin/backup.tsx) — column keys the page never
# reads back, same class as the questions list table above.
src/ui/templates/admin/backup.tsx::backupsTable.key~1353jpe  created → ""   # a column `key` is read only by renderTable's columnKeys/hiddenKeys lookups and saved-filter value rendering; adminBackupPage renders the backups table with none of those options, so this key is never read
src/ui/templates/admin/backup.tsx::backupsTable.key~1o7pzgq  timestamp → ""   # same as backup.tsx's created key — the backups table is rendered without columnKeys, hiddenKeys, or filters, so its column keys are never read
src/ui/templates/admin/backup.tsx::backupsTable.key~1tpb30l  size → ""   # same as backup.tsx's created key — the backups table is rendered without columnKeys, hiddenKeys, or filters, so its column keys are never read
src/ui/templates/admin/backup.tsx::backupsTable.key~120g5gv  actions → ""   # same as backup.tsx's created key — the backups table is rendered without columnKeys, hiddenKeys, or filters, so its column keys are never read
src/ui/templates/admin/settings-statuses.tsx::statusColumns~1m0nc07  flags → ""   # the statuses list renders these columns with only a reorder option, so it supplies no columnKeys, no hiddenKeys, and no filters, and statusColumns never reaches renderColumnReference, so no reader of a column key is in play
src/ui/templates/admin/settings-statuses.tsx::renderStatusFields.value~0evfpwz  ?? → ||   # status?.name is a string or undefined, and the only falsy string is the empty fallback itself
src/ui/templates/admin/settings-statuses.tsx::renderStatusFields.value~053yv47  ?? → ||   # FormParams.get is a string or undefined, and the only falsy string is the empty fallback itself
# attributes list (ui/templates/admin/attributes.tsx) — column keys nothing
# reads back, same class as the statuses table above.
src/ui/templates/admin/attributes.tsx::adminAttributesPage.columns.key~0vgsq6i  attribute → ""   # reorderableListPage renders these columns through renderTable with a reorder option and nothing else, so it supplies no columnKeys, no hiddenKeys, and no filters, and the table never reaches renderColumnReference
src/ui/templates/admin/attributes.tsx::adminAttributesPage.columns.key~0flrsnk  options → ""   # same as the attribute key above — the attributes list supplies only reorder, so no reader of a column key is in play
# groups list, holidays table, logistics agents — three more tables whose
# column keys nothing reads back.
src/ui/templates/admin/groups/list.tsx::groupColumns~0ixy9zk  name → ""   # adminGroupsPage renders these columns with a context and nothing else, so it supplies no columnKeys, no hiddenKeys, and no filters, and groupColumns never reaches renderColumnReference
src/ui/templates/admin/groups/list.tsx::groupColumns~1bp6sm8  slug → ""   # same as the name key above — the groups list supplies only a context, so no reader of a column key is in play
src/ui/templates/admin/holidays.tsx::holidayColumns.key~1lbn6k9  start_date → ""   # HolidayTable renders these columns with a scroll class and nothing else, so no columnKeys, hiddenKeys, or filters read the key, and holidayColumns never reaches renderColumnReference
src/ui/templates/admin/holidays.tsx::holidayColumns.key~0mip5xw  end_date → ""   # same as the start_date key above — the holidays table supplies only a scroll class
src/ui/templates/admin/logistics.tsx::agentColumns~15k9yrk  name → ""   # AgentsSection renders these columns with no options at all, so nothing supplies a column-key reader, and agentColumns never reaches renderColumnReference
src/ui/templates/admin/logistics.tsx::LogisticsAgentEditPanel.html~01ax7pa  ?? → ||   # values is a form-values object or undefined, and an object is always truthy, so || keeps exactly what ?? keeps

# schema-atlas.tsx: formatTimeAgo returns a formatted string or null, never
# "", so ?? and || pick the same side for every input.
src/ui/templates/admin/schema-atlas.tsx::UnansweredMoneySection~15crpim ??→||   # formatTimeAgo → string|null, never ""

# builder.tsx and list-parts.tsx: the two built-sites tables render every
# column with no columnKeys or hiddenKeys passed, so a column's key reaches
# no rendered output.
src/ui/templates/admin/builder.tsx::builtSitesTable~0ytajae name→""   # renderTable(builtSitesTable, sites) filters by key only when columnKeys/hiddenKeys are passed, and this page passes neither
src/ui/templates/admin/builder.tsx::builtSitesTable~0l4ob4i url→""   # same: the key reaches no rendered output on this page
src/ui/templates/admin/builder.tsx::builtSitesTable~0w4j3rw built→""   # same: the key reaches no rendered output on this page
src/ui/templates/admin/built-sites/list-parts.tsx::builtSiteColumns~02alai9 name→""   # same: renderTable(builtSitesTable, sites) passes no columnKeys or hiddenKeys, so no code path reads the column key
src/ui/templates/admin/built-sites/list-parts.tsx::builtSiteColumns~0wl4rmg site_url→""   # same: this table renders with no columnKeys or hiddenKeys
src/ui/templates/admin/built-sites/list-parts.tsx::builtSiteColumns~0g1lcco status→""   # same: this table renders with no columnKeys or hiddenKeys
src/ui/templates/admin/built-sites/list-parts.tsx::builtSiteColumns~1szmvt4 updates→""   # same: this table renders with no columnKeys or hiddenKeys
src/ui/templates/admin/built-sites/list-parts.tsx::builtSiteColumns~1geahz2 read_only→""   # same: this table renders with no columnKeys or hiddenKeys

# seeds.tsx: the page passes no nav route, and no nav section owns the empty
# string, so any non-route value renders the same unhighlighted nav.
src/ui/templates/admin/seeds.tsx::adminSeedsPage~0quyacc  →"mutated"   # the page passes no nav route, and no section owns the empty string, so any non-route value renders the same unhighlighted nav
src/ui/templates/admin/bulk-actions.tsx::adminDuplicateGroupPage.initialRows.dateFind~0jpoy5k  → "mutated"   # computeDayOffset short-circuits when either date is empty, and the initial dateReplace is empty, so the offset stays 0 under either value
src/ui/templates/admin/bulk-actions.tsx::adminDuplicateGroupPage.initialRows.dateReplace~0u00vsl  → "mutated"   # computeDayOffset short-circuits when the initial dateFind is empty, so the replace value is never read
src/ui/templates/admin/bulk-actions.tsx::adminDuplicateGroupPage.initialRows.nameReplace~1eieidw  → "mutated"   # applyNameReplacement short-circuits when the initial nameFind is empty, so the replace value is never read
src/ui/templates/admin/attendees.tsx::adminBlockedRefundAttendeePage~0077u6j  refund → ""   # a disabled confirm page renders no form, so this action segment is never read
src/ui/templates/admin/attendees.tsx::EditQuestions.textValue~0a2e8nn  ?? → ||   # a stored free-text answer is a string or undefined, and the "" fallback equals what || gives for the empty string
src/ui/templates/admin/api-keys.tsx::apiKeyColumns~171hyss  name → ""   # the key table renders without columnKeys, hiddenKeys, or filters, so no code path reads its column keys
src/ui/templates/admin/api-keys.tsx::apiKeyColumns~1pdocf9  created → ""   # same as the name key — the key table renders without columnKeys, hiddenKeys, or filters
src/ui/templates/admin/api-keys.tsx::apiKeyColumns~13k1qvy  last_used → ""   # same as the name key — the key table renders without columnKeys, hiddenKeys, or filters

# renewal-tier summary table (built-sites) — same class as the key table above:
# RenewalTierSummary renders it with no options at all.
src/ui/templates/admin/built-sites/renewal-summary.tsx::renewalTierColumns~035yw77  tier → ""   # the tier table renders without columnKeys, hiddenKeys, or filters, so no code path reads its column keys
src/ui/templates/admin/built-sites/renewal-summary.tsx::renewalTierColumns~1cvsdgu  months → ""   # same as the tier key — the tier table renders without columnKeys, hiddenKeys, or filters
src/ui/templates/admin/built-sites/renewal-summary.tsx::renewalTierColumns~1qd3mhi  price → ""   # same as the tier key — the tier table renders without columnKeys, hiddenKeys, or filters
src/ui/templates/admin/built-sites/renewal-summary.tsx::renewalTierColumns~0pama76  units → ""   # same as the tier key — the tier table renders without columnKeys, hiddenKeys, or filters

# availability checker table — same class as the renewal tiers above: the
# checker renders it with only a table class, so nothing reads a column key.
src/ui/templates/admin/availability-checker.tsx::availabilityColumns.key~1i3cwde  select → ""   # the availability table renders without columnKeys, hiddenKeys, or filters, so no code path reads its column keys
src/ui/templates/admin/availability-checker.tsx::availabilityColumns~1icoqk7  listing → ""   # same as the select key — the availability table renders without columnKeys, hiddenKeys, or filters
src/ui/templates/admin/availability-checker.tsx::availabilityColumns~15bbaul  remaining → ""   # same as the select key — the availability table renders without columnKeys, hiddenKeys, or filters
src/ui/templates/admin/availability-checker.tsx::availabilityColumns~1ra2ram  price → ""   # same as the select key — the availability table renders without columnKeys, hiddenKeys, or filters

# attendee detail — booking column keys the bookings table never reads back
# (it renders with only a context and a foot row), and two fallbacks whose
# left side can never be falsy-but-non-null.
src/ui/templates/admin/attendee-detail.tsx::bookingColumns~1cgjcue  listing → ""   # the bookings table renders without columnKeys, hiddenKeys, or filters, so no code path reads its column keys
src/ui/templates/admin/attendee-detail.tsx::bookingColumns~1tza93f  date → ""   # same as the listing key — the bookings table renders without columnKeys, hiddenKeys, or filters
src/ui/templates/admin/attendee-detail.tsx::bookingColumns~15kw9kh  quantity → ""   # same as the listing key — the bookings table renders without columnKeys, hiddenKeys, or filters
src/ui/templates/admin/attendee-detail.tsx::bookingColumns~029b0v4  status → ""   # same as the listing key — the bookings table renders without columnKeys, hiddenKeys, or filters
src/ui/templates/admin/attendee-detail.tsx::bookingColumns.parent~0t7jibv  ?? → ||   # nameByListingId holds listing names, and a listing name is required and saved non-empty, so the left side is undefined or a non-empty string — the empty fallback is the only falsy string, and it cannot occur
src/ui/templates/admin/attendee-detail.tsx::bookingColumns~1dkqpm3  ?? → ||   # BookingStatusBadges returns a div element or null, and an element object is always truthy, so ?? and || pick the same side
src/ui/templates/admin/attendee-detail.tsx::AttendeeBookingsTable.names~1d4a28o  ?? → ||   # childNamesByParentId.get() returns string[]|undefined, and an array is always truthy, so ?? and || keep exactly the same entry

# attendee-form listing editor — column keys the editor table never reads
# back, and two fallbacks whose left side can never be falsy-but-non-null.
src/ui/templates/admin/attendee-form/listing-editor.tsx::listingColumns~0b4eb34  listing → ""   # the listing editor table renders without columnKeys, hiddenKeys, or filters, so no code path reads its column keys
src/ui/templates/admin/attendee-form/listing-editor.tsx::listingColumns~1pp18xr  dates → ""   # same as the listing key — the editor table renders without columnKeys, hiddenKeys, or filters
src/ui/templates/admin/attendee-form/listing-editor.tsx::listingColumns~0pvuhzr  quantity → ""   # same as the listing key — the editor table renders without columnKeys, hiddenKeys, or filters
src/ui/templates/admin/attendee-form/listing-editor.tsx::listingColumns.key~0s9jed5  notices → ""   # same as the listing key — the editor table renders without columnKeys, hiddenKeys, or filters
src/ui/templates/admin/attendee-form/listing-editor.tsx::pathLabel.name~0i58gz8  ?? → ||   # parentNamesById holds listing names, and a listing name is required and saved non-empty, so the left side is undefined or a non-empty string — the falsy fallback cannot occur
src/ui/templates/admin/attendee-form/listing-editor.tsx::listingNoticesCell~0s46002  ?? → ||   # lineWarnings.get() returns string[]|undefined, and an array is always truthy, so ?? and || keep exactly the same entry

# servicing pages (features/admin/servicing) — three tables whose column keys
# nothing reads back (each renderTable call passes no columnKeys, hiddenKeys,
# or filters), and fallbacks whose left side can never be falsy-but-non-null.
src/features/admin/servicing/page.tsx::servicingListingsTable~0nw80un  listing → ""   # the servicing listings table renders without columnKeys, hiddenKeys, or filters, so no code path reads its column keys
src/features/admin/servicing/page.tsx::servicingListingsTable~0yujpzb  quantity → ""   # same as the listing key — the servicing listings table renders without columnKeys, hiddenKeys, or filters
src/features/admin/servicing/page.tsx::servicingCostsTable~0odr3ie  listing → ""   # the costs table renders without columnKeys, hiddenKeys, or filters, so no code path reads its column keys
src/features/admin/servicing/page.tsx::servicingCostsTable~0ddbrbs  date → ""   # same as the listing key — the costs table renders without columnKeys, hiddenKeys, or filters
src/features/admin/servicing/page.tsx::servicingCostsTable~1di7dor  amount → ""   # same as the listing key — the costs table renders without columnKeys, hiddenKeys, or filters
src/features/admin/servicing/page.tsx::servicingCostsTable~0727n96  note → ""   # same as the listing key — the costs table renders without columnKeys, hiddenKeys, or filters
src/features/admin/servicing/page.tsx::servicingCostsTable~0g8rkej  actions → ""   # same as the listing key — the costs table renders without columnKeys, hiddenKeys, or filters
src/features/admin/servicing/page.tsx::servicingEventsTable~1a6aadf  name → ""   # the events table renders without columnKeys, hiddenKeys, or filters, so no code path reads its column keys
src/features/admin/servicing/page.tsx::servicingEventsTable~0gqe9hj  date → ""   # same as the name key — the events table renders without columnKeys, hiddenKeys, or filters
src/features/admin/servicing/page.tsx::servicingEventsTable~06l76zw  listings → ""   # same as the name key — the events table renders without columnKeys, hiddenKeys, or filters
src/features/admin/servicing/page.tsx::servicingEventsTable~1di7ynd  quantity → ""   # same as the name key — the events table renders without columnKeys, hiddenKeys, or filters
src/features/admin/servicing/page.tsx::firstBookingDate~1fk4ln1  ?? → ||   # find() only returns a booking whose date is a truthy string, so the found side is never falsy and ?? and || agree on every input
src/features/admin/servicing/page.tsx::firstBookingDuration~115f2gu  ?? → ||   # find() only returns a booking whose durationDays is a truthy number, so the found side is never falsy and ?? and || agree on every input
src/features/admin/servicing/page.tsx::listingFormQuantities~1sgnzg4  ?? → ||   # event?.bookings is an array or undefined, and an array is always truthy, so ?? and || keep exactly the same list
src/features/admin/servicing/page.tsx::costListingDisplay.name~0u6nohx  ?? → ||   # costListingNames holds listing names, and a listing name is required and saved non-empty, so the left side is undefined or a non-empty string — the fallback cannot differ
src/features/admin/servicing/page.tsx::servicingEventsTable~0rgbx6b  ?? → ||   # the listings join reads fieldById("name") of listing rows, and a listing name is required and saved non-empty — the left side is undefined or a non-empty string, so ?? and || agree
src/features/admin/servicing/page.tsx::renderServicingPage.html.name~1q0cbpf  ?? → ||   # event?.name is undefined or a stored event name, and an event name is required, so the left side can never be the "" fallback's only falsy string
src/features/admin/servicing/page.tsx::renderServicingPage.title~0y52sh9  ?? → ||   # event?.name is undefined or a stored event name, and an event name is required, so ?? and || pick the same title for every event

src/ui/templates/admin/listings/overview.tsx::overviewStatsFromAttendees~0so9vc9  ?? → ||   # the operand is a Set or undefined, and every Set object is truthy

src/ui/templates/public/reservations/listing-rows.ts::renderListingRow.hideQuantity~0u0j56w  false → true   # buildListingRows holds renderListingRow's only call site, and it passes hideQuantity explicitly, so the default parameter is never read
