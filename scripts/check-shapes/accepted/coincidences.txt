# Functions that share a shape and are not the same thing.
#
# A shape is a coarse thing: build an object from another object's fields, map
# then join, compare two fields with `&&`. Enough code does that for two
# unrelated functions to match without either being a copy of the other.
#
# A line belongs here only when there is no shared step to lift, and only when
# no two of its sites are a pair. When in doubt, try writing the curry first —
# "these two cannot be merged" is nearly always wrong, and this file is the one
# place that judgement is recorded.
#
# Format: the match, two spaces, then `# why they are not one thing`.

e2e-payments/src/cucumber/steps/pages.ts::bookingIdentity,src/features/admin/refunds/candidates.ts::loadedRefundAttendee  # each builds an object from another object's fields, one in the app and one in the live-payment harness
scripts/specs/evidence/resolve.ts::stableItem,src/features/admin/holidays.ts::extractHolidayInput,src/features/admin/modifiers.ts::scopeLinksFor.options@1,src/features/admin/scanner.ts::handleScannerGet.uncheckedIn@2,src/features/public/group-liveness.ts::publicGroupSummary,src/shared/db/attendees/tokens.ts::previousBookingLineWithoutAttendee,src/shared/db/attributes.ts::rowAttribute,src/shared/listing-details.ts::listingDetails,src/shared/provider-refunds/state.ts::receipt,src/ui/templates/admin/bulk-actions.tsx::adminDuplicateGroupPage.listingsData,src/ui/templates/admin/ledger/entry-form.ts::ledgerEntryAddFields.options  # eleven functions that each build an object from another object's fields; that is construction, not a shared step
src/features/admin/content-form-fields.ts::seoContentInput,src/features/api/payment-processing/snapshot/io.ts::loadPaidOrderSnapshot.dayPrices,src/features/instance.ts::handleSiteCredentials.credentials,src/shared/db/contact-preferences.ts::statsToBlob,src/shared/db/logistics.ts::rowToAssignment,src/shared/stripe/endpoints.ts::testStripeConnection.webhooks  # six functions that each build an object from another object's fields
src/features/admin/listing-qr.ts::loadQrFormContext,src/shared/db/payment-anchor/reference.ts::paymentAnchorReference  # both destructure a Promise.all of two unrelated loads
src/features/admin/settings-general.ts::handleThemePost.extract,src/features/admin/settings-square.ts::squareRoutes.extraFields  # both read a settings form into an object; the fields differ and nothing else is shared
src/shared/db/chosen-columns.ts::chooseColumns.columnsSql,src/shared/markdown.ts::rewriteTokens  # both are `xs.map(fn).join(sep)` over unrelated data. What they share is two language built-ins and no call at all, so a mapJoin helper would only put a name in front of syntax every reader knows
src/shared/ledger/reverse.ts::isInverseOf,src/shared/provider-refunds/state.ts::sameConflictDecision  # both compare three fields with &&, over a ledger leg and over a refund decision
src/shared/payment/refund-machine-spec.ts::family,src/ui/templates/fields/aggregate.ts::buildAggregateFields  # a map with a destructured entry, over machine states and over field specs
src/ui/templates/admin/settings/schema-form.tsx::renderField,src/ui/templates/admin/settings/schema-form.tsx::settingsForm  # two switch statements over unrelated unions in one file

src/shared/email.ts::sendgridAttachment,src/shared/payment/transport-error.ts::providerDetail.stripe  # each names four fields of its own, one for a SendGrid attachment and one for a Stripe error; the field names are the whole content, so there is nothing to lift
src/ui/templates/components/aggregate-sections.tsx::SectionFieldset,src/ui/templates/components/page-structure.tsx::TitledArticle  # both wrap children under a heading, but in different elements: a form section is a fieldset with a legend, which is the accessibility contract AGENTS.md pins, and an article is an h2. A curry over the tag name would hide exactly the thing those rules exist to keep visible
src/features/admin/built-site-page.tsx::secretsTab,src/features/admin/built-site-page.tsx::updateTab  # two tabs that each load one thing for a site and hand it to a panel; the panels name that thing differently, view and state, and a curry could only share the await by renaming both to one word that says less
